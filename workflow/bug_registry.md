---
gmd: "0.1"
id: bug_registry
title: "Bug Registry — known bugs, root causes, and fixes"
tags: [process, bugs, governance]
metadata:
  node_type: registry
---

# Bug Registry {#root}

Curated log of bugs encountered and fixed — sibling to the mock and deferral registries. Where
[[test_mock_registry]] classifies test-side mocks and [[deferral_registry]] tracks deferred
functionality, this registry captures **bugs**: what broke, why, and the fix — so the same mistake
is not re-derived across sessions. Managed the same way (curated, classified, GMD-linted);
`@ch-gap-master` keeps it current, `@ch-bsd` mines it for recurring cheap-fix patterns.

rel: part-of -> [[test_mock_registry]]

## When to log {#when}

The threshold is LOW — when in doubt, log it. Log an entry whenever:

- The user reports an error, bug, or problem ("doesn't work", "broken", "shows wrong X").
- A test, build, lint, or type check fails, or a runtime/import/type/syntax error occurs.
- You fix something that was broken, or change error-handling / validation logic.
- You edit the same file more than twice to get one thing right (a signal it was a bug).

**Before fixing:** search this registry first — the fix may already be known. If `rmx` is present,
`rmx memory search "<symptom>"` also surfaces past occurrences.

## Status {#status}

| Status | Meaning |
|--------|---------|
| `open` | Reproduced, not yet fixed |
| `fixed` | Fix landed + verified |
| `recurring` | Seen again after a prior fix — needs a durable/root fix |

## Registry {#registry}

| ID | Symptom / error | File(s) | Root cause | Fix | Tags | Status | Seen |
|----|-----------------|---------|------------|-----|------|--------|------|
| bug-001 | deploy venv `.pth` → dev checkout; `rmx`/daemons/hub ran uncommitted code (2026-09-14) | `~/refmatrix/.venv/.../__editable__.refmatrix-*.pth`, save-state memory step 4 | absolute `pip install -e <dev tree>` into the deploy venv | `upgrade.runtime_identity` + `verify_editable`; `rmx version -v`; memory rewritten (0.66.3) | deploy, venv | fixed | 1 |
| bug-002 | `rmx install-hooks --enforce` accepted but ignored (want() only honoured it with no project root) | src/refmatrix/hooks.py | forced branch unreachable from every production caller | `if enforce is True: return True` (0.68.1) | hooks | fixed | 1 |
| bug-003 | `--force` stripped user hooks named `enforce-*.sh` | src/refmatrix/hooks.py `_RMX_HOOK_SIGNATURES` | prefix signature `.claude/hooks/enforce-` | exact script names (0.68.1) | hooks | fixed | 1 |
| bug-004 | daemon wedged 300 s adopting a mute `models.sock`; `test_daemon_falls_back_when_shared_socket_does_not_answer` timed out | src/refmatrix/modelsrv.py, daemon.py `_model_client` | probe used the worker op timeout; a socket timeout (an OSError) triggered reconnect with the default timeout | bounded `info(timeout=PROBE_TIMEOUT_S)`, no retry on TimeoutError (0.68.1) | daemon, models | fixed | 1 |
| bug-005 | Claude Code shell snapshots contained `set -o #`; user function bodies had grep rewritten to rmxgrep | bin/rmxgrep, hooks.AGENT_BASHRC_SECTION, cli.py `_grep_stdin_addendum` | BASH_ENV alias + expand_aliases + RMXGREP_MODE=rich in the snapshot shell; index note on stdout | note → stderr (0.66.2); piped stdin = real grep; grep()/rg() functions (cf3d87d) | grep, hooks | fixed | 1 |
| bug-006 | `rmx ingest-gmd --detach` said "no daemon running" to a busy daemon; SessionStart catch-up skipped after every deploy | src/refmatrix/cli.py ingest_gmd | ping cannot tell busy from absent | discovery.daemon_status busy → retry then busy-specific error (0.68.1) | daemon, bridge | fixed | 1 |
| bug-007 | `rmx_where` / `rmx_locate` / `rmx locate` returned stale (or empty) results from a long-lived process after the store changed; `test_locate_verb_finds_an_ingested_file_by_basename` → `{'results': []}` | src/refmatrix/search.py `cached_replica` | the cached read-only DuckDB connection stays bound to the OLD `catalog.read.duckdb` inode after the daemon's tmp+rename snapshot; the MCP server / hub never reopen | `_snapshot_sig` (ino, mtime_ns, size) recorded at open; `cached_replica` reopens when it changes (plan-3 r1 remedy) | search, replica, staleness | fixed | 1 |
| bug-008 | `tests/test_repo_hooks_in_sync.py` red: `script rmxgrep-rewrite.py differs from the generator's render` — live `~/.claude/hooks/rmxgrep-rewrite.py` carried `RMXGREP = "/Users/tholley/claude_tools/refmatrix/bin/rmxgrep"` (dev tree) at 21:43 after a 21:31 deploy regen | src/refmatrix/search_hooks.py `install_search_hooks` / `render_scripts` | the rewriter bakes the GENERATOR's tree into a USER-GLOBAL script; any `install-hooks --apply` from the dev venv (a test without the `RMX_CLAUDE_HOOKS_DIR` redirect, an audit probe) overwrites the live hook with dev paths | DURABLE FIX SHIPPED (task 6.5, 2026-09-16). Two defences: (a) `RMXGREP`/`RMXRG` became functions resolving from the tree that owns the `rmx` on PATH, so the rendered bytes no longer depend on the generating tree — proven by rendering from the DEV tree and executing it, which resolves `/Users/tholley/refmatrix/.venv/bin/rmxgrep` (deploy); (b) `install_search_hooks(apply=True)` refuses the user-global dir when `runtime_identity()['venv_tree']` != `path_rmx_tree()`, naming both trees and the `RMX_CLAUDE_HOOKS_DIR` escape. 24 tests; 2 mutations. **NOT verified live:** `rmx` on PATH is the 0.69.1 deploy build and reports in sync about the OLD render; the live hook still carries `RMXGREP = "/Users/tholley/refmatrix/bin/rmxgrep"`, which points at the DEPLOY tree and so is correct today, not drifted. Converts on the next deploy + `install-hooks --apply --force` | hooks, dev-deploy | fixed 2026-09-16 | 2 |
| bug-009 | pytest died silently mid-run (no summary line) after `test_daemon_learn_guard` — `...........F.........` then nothing | tests/test_daemon_learn_guard.py, src/refmatrix/daemon.py `_arm_deferred_exit` | the degrade path arms a deferred `os._exit` thread (2 s); the test's `os._exit` monkeypatch was undone before it fired, so the REAL exit killed the test process | the test sets `RMX_DEGRADE_EXIT_S=0.2` and asserts the deferred exit inside its patch; also `test_hub_watchdog` now records `hub.graceful_stop` instead of letting it signal a fake pid | tests, daemon | fixed | 1 |
| bug-010 | full suite at e1dba80: `test_coref.py::test_as_memory_ingest_emits_coref_linkage` → `assert None is not None` (row looked up as `note.md`); `test_phase8_mcp_sched.py::test_federated_concept_groups_by_project` → `assert set() == {'a', 'b'}` | tests/test_coref.py, tests/test_phase8_mcp_sched.py | production changes outran their tests' fakes: plan-5 5.1 (3039d2e) names bridged memories by stem, and plan-3 r2 moved `_live_roots` from `daemon_mod.ping` to `discovery.daemon_status` (busy ≠ absent) — the test patched the old seam, so the real probe ran against `/a/.refmatrix` and classified it absent | tests look up `note` and patch `search.discovery.daemon_status`; the fake-signature sweep is the plan-6 6.4 registry pass | test-fake, seam-drift, plan-6 | fixed | 1 |
| bug-011 | SessionStart in nine projects: `compile_guardrails: rmx memory sync-disk … exited 2: No such command 'sync-disk'` behind `>/dev/null 2>&1 \|\| true` — edited guardrails never reached the store; plus two daemon boots died on `Could not set lock on file catalog.B.duckdb: Conflicting lock is held` | src/refmatrix/discovery.py `daemon_status`, src/refmatrix/cli.py `memory_sync_disk` / `_store` / `_ingest_gmd_sync`, src/refmatrix/hooks.py `_add_enforce_entries`, .claude/p20-0/compile_guardrails.py | (a) `daemon_status` said busy only when the socket file existed, but `serve_forever` binds the socket LAST — the boot window (pid alive, Store open, no socket) classified absent and the CLI opened the slot in-process; (b) plan-5 deleted `memory sync-disk` without sweeping callers, and the generated hook silenced the compiler | (a) `pid_is_rmx` (live pid + command line names rmx/refmatrix) → busy with or without socket; `daemon status` renders `starting — socket not bound yet`; (b) alias restored as a thin forward to the bridge, compiler calls `ingest-gmd --as-memory` and fails the compile on a failed seed, SessionStart entry is a plain `if` with no redirect; tests `tests/test_plan5_remedy.py` | busy-not-absent, boot-window, silent-hook, sync-disk | fixed | 1 |
| bug-012 | `test_stop_daemon_signals_first_when_ping_fails` → `stop_daemon` returned False after 7.5 s with `kill: SIGKILL (heartbeat infs stale)` although the SIGTERM'd child was dead | src/refmatrix/daemon.py `is_alive` | `os.kill(pid, 0)` succeeds on a ZOMBIE (our own exited child before `wait`), so a signalled child daemon looked alive for the whole grace and drew the stale-heartbeat SIGKILL | `is_alive` first tries `os.waitpid(pid, WNOHANG)` — reaps/sees an exited child; a foreign pid (launchd's daemons) falls to the signal probe | zombie, liveness, plan-4 | fixed | 1 |
| bug-013 | after `rmx hub relaunch-fleet` (098774a) the project and global labels were UNLOADED (`launchctl print` rc 113) while their daemons ran standalone (ppid 1, no adoption); the plist files on disk were current | src/refmatrix/launchctl.py `install` / `_wait_loaded`, src/refmatrix/cli.py `daemon_restart` (relaunch) | `install(force=True)` bootouts a RUNNING label, waits 3 s for it to leave the domain and IGNORES the result; the bootstrap then fails against the still-loaded (draining) label and the `load` fallback too, and `_wait_loaded(True)` only proves *something* is loaded; a moment later the old job finishes booting out → nothing loaded. `daemon restart --relaunch` then took the not-loaded branch and spawned a standalone daemon | `install(force)` waits for the bootout to complete (up to the kill grace) and raises if the label is still loaded; after bootstrap it re-reads `launchctl print` and requires the rendered env; `check` compares the LOADED job's env too; `relaunch` on an installed-but-unloaded plist bootstraps it instead of spawning standalone. Live fix 2026-09-15 01:33: plain `install` on both roots → supervised daemons adopted the standalone ones | launchd, install-force, plist, plan-4 | fixed | 2 |
| bug-014 | every daemon logs `embed: hub-shared worker unusable (TimeoutError('timed out')); falling back to a private worker` at boot; `hub.log`: `models worker[rerank] warm in 2195.3s`, repeated `models role=rerank op=info failed: BrokenPipeError`; fleet back on 16 private workers (~450 MB each) | src/refmatrix/modelsrv.py `SharedWorkerClient` (daemon side), src/refmatrix/hub.py models supervisor | the daemon probes the shared worker with a 5 s timeout, but a COLD worker (first spawn, or re-created after the 900 s idle-evict) needs 10–20 s to load its model (36 min under the benchmark's CPU saturation), so every daemon that boots or reconnects while the worker is cold falls back to a private worker for good; the hub's `info` op on a still-loading worker breaks the pipe | ROOT CAUSE (2026-09-15 02:35, hub.log): the `BrokenPipeError` lines are the hub failing to SEND the reply to a daemon whose bounded probe (5 s) had already given up while the worker was still loading (measured warm: embed 14.9 s, rerank 33 s) — the worker was never broken. Fix: `PROBE_TIMEOUT_S` 5 → 45 s so a booting daemon waits for a cold worker instead of going private; `ModelServer._handle` treats a client-side send failure as "client left" (worker kept) and drops a worker only when ITS pipe failed after `WorkerClient`'s own respawn-and-retry, and only that worker (a stale reference must not close the replacement — the bug-015 follow-up's first version did exactly that and cascaded across the fleet) | models, hub, memory-footprint, cold-start | fixed | 2 |
| bug-015 | the project daemon wedges minutes after boot: ping answers, heartbeat goes stale (88 s), `rmx partition list` > 25 s, `hub queues` says busy, no job lines in rmxd.log; the hub watchdog restarted it 01:47, 01:53, 02:26 (`graceful stop … not answering ping (SIGTERM sent now)`); every hook probe in the window returned `[]` | src/refmatrix/daemon.py (`_reranker` / `_model_client`, the rerank under an op), src/refmatrix/modelsrv.py (hub worker proxy), src/refmatrix/scan.py `scan_prompt` (`shared_reranker()` unbounded) | hypothesis (no stacks: the daemon has no dump signal): the hub's shared rerank worker is broken (`models role=rerank op=rerank failed: BrokenPipeError` on every call) and a daemon-side rerank inside an op waits on it at the worker's 300 s default while holding the store lock; the CLI's `scan-prompt` blocks on the same worker for 20 s+ (killed at 20 s in the probe) | remedied (root cause still a hypothesis; bug-014's cold-worker finding makes the daemon-side rerank-under-lock on a cold/mute worker the leading one): `SHARED_OP_TIMEOUT_S` (30 s) on the daemon's shared-worker client, `scan-prompt --timeout 5` (rerank probe 1 s) in the generator, `ModelServer._drop_worker` on a broken pipe, `kill -USR1 <pid>` dumps every thread's stack to daemon.stderr.log — `workflow/implementation_summaries/bug-015-daemon-wedge.md`; the next wedge gets a dump | wedge, rerank, shared-worker, heartbeat | remedied | 3 |
| bug-017 | `test_recall_hook_wall_is_within_budget_on_a_held_writer` answered `[]` with an empty stderr (no busy word) once the partition probe read the replica first; first seen as order-dependent (the `Store.__init__` spy fired on a "bare" root that was in fact the seeded one) | tests/test_plan2_remedy.py (`_SilentDaemon(seeded=True)`), tests/test_plan3_remedy_r4.py | the seeded held-writer simulation created its catalog and CLOSED it, so the legacy `catalog.duckdb` was readable as a replica — a live daemon's exclusive lock refuses that read-only open; the fixture was less faithful than the daemon it simulates, and the replica-first probe exposed it | the fixture holds its writer Store open for its lifetime (same-process DuckDB raises `Can't open a connection to same database file with a different configuration` on the read-only open — the lock conflict's in-process twin); the replica test snapshots first. Mutation C (`pytest-partition-probe-mutation.log`) proves the fidelity is load-bearing | fixture-fidelity, held-writer, replica-first, order-dependent | fixed | 1 |
| bug-018 | `test_verb_call_distinguishes_busy_from_absent` asserted `pid={os.getpid()}` in the busy error and failed with the lookalike child's pid | tests/test_plan3_remedy_r2.py | the plan-4 r2 remedy pointed `_SilentDaemon`'s pid file at a child process whose argv carries `rmx` (the ps race); the plan-3 r2 test still expected pytest's own pid and was not in the runs since | the assertion compares against `silent.pid` | test-drift, busy-absent | fixed | 1 |
| bug-016 | 21 `-p global daemon start --no-watch` processes alive (ppid 1) on deleted `/tmp/rmxh-*` roots, each holding a catalog + socket, from 21:09 to 01:57 (ch-bsd plan-5 r3 observation) | tests/test_verbs_migrated.py `home` fixture | tests that reach the global store spawn a global daemon under the tmp `RMX_HOME` (`hub.ensure_global_daemon`); the fixture's teardown only removed the directory | the fixture stops the global daemon under its home before `rmtree`; the 21 leaked daemons were SIGTERMed 2026-09-15 03:2x (deleted roots, never live stores) | test-leak, global-daemon, fixture | fixed | 1 |
| bug-019 | the per-prompt recall hook's rerank leg alternated "rerank failed (TimeoutError)" / "rerank skipped: shared worker unavailable" on an idle fleet while a bare probe answered in 10 ms | src/refmatrix/cli.py `_replica_memory_recall`, src/refmatrix/reranker.py `collect_rerank_docs` | the pool as extracted (10 memory docs, 28.7k chars, max 4.2k) costs the shared cross-encoder 4.8 s — the whole 5 s hook — and the worker keeps scoring after the client's deadline, so the next hook's 1 s probe queues behind it | `collect_rerank_docs(doc_chars=)`; the hook passes `RERANK_DOC_CHARS`=700 (measured: 768 → 3.4 s, 512 → 2–3 s); todo G13 for the worker-reported cost. RECURRED 2026-09-15: the capped pool still costs 3.7-14.1 s under ordinary load and the leg times out every run - see bug-025, which supersedes this fix | rerank, shared-worker, hook-budget | recurring | 2 |
| bug-020 | launchd SIGKILLed five draining daemons in one night (`Service did not exit 5 seconds after SIGTERM. Sending SIGKILL.`), every one during `relaunch-fleet`; the boot-time index repair ran on every project boot | src/refmatrix/launchctl.py `render_plist`, src/refmatrix/cli.py `daemon restart` | the plist set no `ExitTimeOut` (launchd default 5 s) while the daemon's drain is up to ~50 s; the deploy step was a bare `kickstart -k` with no stop order first (ch-bsd plan-4 r3 #b-1) | `ExitTimeOut` = `EXIT_TIMEOUT_S` (45) on every plist (relaunch-fleet re-renders on drift); `daemon restart` stops gracefully before the kick and kicks without `-k` when it stopped | launchd, sigkill, deploy | fixed | 1 |
| bug-021 | `Watchdog._await_shutdown` returned in 0.0 ms from the only state its caller produces (heartbeat > 60 s stale), so the r2 remedy for the mid-drain SIGKILL never waited | src/refmatrix/hub.py `_await_shutdown`, src/refmatrix/daemon.py | the guard read the heartbeat, which the shutdown stops as its first act — a value the caller had already ruled out (pattern guard-cannot-fire, 4th sighting) | the daemon writes `shutdown.started`; the watchdog waits on that marker bounded by `SHUTDOWN_BUDGET_S`; the test runs the CALLER (`_check`) on a real draining process | watchdog, guard-cannot-fire, shutdown | fixed | 1 |
| bug-022 | after `rmx hub launchctl install --force` printed `hub supervised`, `launchctl list` had no `com.refmatrix.hub` and `rmx hub status` said not running (deploy 2026-09-15 05:27); the fleet then relaunched with the hub down | src/refmatrix/launchctl.py `install_hub` | bug-013's shape in the hub twin: a 3 s bootout wait, then a bootstrap that landed on a job still unloading (read as loaded), then launchd finished the bootout and removed the label | `install_hub` waits `BOOTOUT_WAIT_S` for the domain to release the label (error, never a shrug), bootstraps, settles `HUB_SETTLE_S` and re-checks, bootstrapping once more when the label vanished; three state-machine tests | launchd, bootstrap-race, hub, quoted-line-sibling | fixed | 1 |
| bug-023 | a real LaunchAgent `com.refmatrix.daemon.rmxp4-…` appeared for a deleted pytest tmp root (program = the test's fake `bin/rmx`), loaded, `runs = 1` | tests/test_plan4_remedy_r3.py `test_install_refuses_a_dev_tree_binary` | the test faked nothing below the refusal, so mutation P4-G (the refusal removed) let `launchctl.install` write and bootstrap a real plist — a test able to reach launchd when the code under test is wrong | every launchd surface is faked in that test (`plist_path`, `LAUNCH_AGENTS_DIR`, `_migrate_legacy`, `is_loaded`, `subprocess.run`), asserting zero `launchctl` calls; the stray label was booted out and its plist removed | test-hygiene, mutation-side-effect, launchd | fixed | 1 |
| bug-024 | after the fleet relaunched while the hub was down (bug-022), every daemon booted with PRIVATE embed + rerank workers (16 model processes = bug-014's count) and kept them after the hub returned; the shared rerank worker then scored 10 × 700-char docs in 16–25 s (0.7 s for one), and every per-prompt hook rerank timed out | src/refmatrix/daemon.py `_model_client` (the adoption probe runs once at boot) | a daemon that goes private never re-probes the shared worker; N torch processes on one CPU oversubscribe its threads (the reason sharing exists) | state: `relaunch-fleet` again with the hub up (2 workers); code: todo G14 — periodic re-adoption of the shared worker, and the hub's outage must not be a permanent split | shared-worker, private-fallback, cpu-contention | open (G14) | 1 |
| bug-025 | the deployed per-prompt recall hook reranks nothing under ordinary load: 7/7 runs of the exact hook argv burned the full 5 s budget (5.23–5.27 s) with `rerank failed (TimeoutError: timed out)` and 0 of 5 rows reranked, returning exactly what `--no-rerank` returns in 0.79 s (ch-bsd plan-2 r9, 05:48–06:02, load 3.5–5.8) | src/refmatrix/cli.py `_replica_memory_recall` (`RERANK_DOC_CHARS`=700), src/refmatrix/reranker.py `collect_rerank_docs` / `apply_rerank`, docs/architecture/todo.md G13 | bug-019's fix capped the pool until ONE measurement fit; the cost was never made knowable to the caller. Real memory prose at 700 chars is the expensive case — the identical capped 10×700 pool sent straight to `models.sock` took 9.26 / 14.08 / 3.72 s on three consecutive tries (synthetic filler at the same size: 1.00 s), and one serialized worker serves every hook of every session. The 1.7–2.8 s acceptance number was taken 40 s after `relaunch-fleet`, the quiet window | OPEN. Real fix = G13: `info` returns the worker's EMA seconds-per-doc (or queue depth), the leg SKIPS with a said reason when `docs × cost > remaining` instead of timing out, plus a deadline in the `rerank` frame so an abandoned request stops starving the next caller. G13 has NO task spec — must get one (rule 9a). Honest interim: `RMX_RERANK=0` on the per-prompt hook. Acceptance must count reranked rows under ordinary load, two runs minutes apart — never wall time in the quiet minute | rerank, shared-worker, hook-budget, cost-measured-idle, plan-2 | open | 1 |
| bug-026 | `rmx memory get <name> --degree 1` prints the body then dies on the DEPLOYED 0.69.1, in every daemon state: `NameError: name 'daemon_mod' is not defined. Did you mean: 'daemon_job'?` at cli.py:9708 | src/refmatrix/cli.py:9708 (`if daemon_mod.ping(root):` in `memory_get`'s `--degree` tail) vs :9662-9690 (the r5-rewritten body) | the plan-3 r5 remedy (a035117, in dfc0e62) routed `memory get`'s daemon branch through `_verbs.memory(action="get", …)` and dropped the function-local `from refmatrix import daemon as daemon_mod`; the `--degree` tail thirty lines below still calls it, and there is no module-level binding. 169 tests pass at HEAD because NO test invokes the flag | FIXED 763aaa0 (round 7): the tail routes through `verbs.attach_context` — the bundle verb the recall twin already calls, so the capability has ONE implementation — bounded by `MEMORY_CONTEXT_TAIL_S` (30 s) and skipped entirely when the read leg already fell to the replica. Three tests; mutation A (the tail back to its own `daemon_mod.ping`) kills all three | dead-flag, verb-migration-residue, plan-3 | fixed | 1 |
| bug-027 | `rmx memory promote <name>` on a held writer waits **180.2 s** and exits 1 with EMPTY output (unhandled `TimeoutError`, raw traceback); the verb twin `verbs.memory(…, "promote")` waits 90.2 s (3 × 30 s) before `VerbBusyError` — 3× its own stated bound | src/refmatrix/cli.py:9744-9750 (`memory_promote` behind a bare `daemon_mod.ping`), :9513-9525 (`_memory_daemon_call` default `timeout=60.0, retries=2`), src/refmatrix/verbs.py:1100-1104 (`_call(…, timeout=30.0)` at default `retries=2`) | plan-3 r6 bounded `memory list` + `search` through the verb because a held writer answers ping, and left `promote` — the group's third read path through the same `memory_get` op behind the same gate — untouched. Q11 says the promote read uses `retries=0`; the code takes the library default. `grep -n "_verbs.memory(" src/refmatrix/cli.py` → get, list, search; never promote | FIXED 763aaa0 (round 7): the twin calls `_verbs.memory(action="promote", timeout=MEMORY_READ_BUDGET_S)` and renders the typed error; in the verb `budgeted = is_read or action == "promote"` puts the project-side read under the deadline at `retries=0`. NO replica fallthrough — promote writes what it read into the global store. Three tests; mutation B (back behind the bare ping) fails them in 240 s, reinstating the hang | partial-bound, verb-bypass, held-writer, plan-3 | fixed | 1 |
| bug-028 | `rmx hub launchctl install` writes the hub's plist from `which rmx` and refuses nothing: with `RMX_BIN` pointed at a binary answering `dev_tree: true`, `hub ProgramArguments[0]` = the dev binary, unrefused — and no `check_hub` exists, so a hub plist aimed at a dev tree survives every deploy silently | src/refmatrix/launchctl.py:249-253 (`render_hub_plist(*, port, host)` → `_rmx_path()`), :334-390 (`install_hub` — no `rmx=`, no `_refuse_dev_tree`, no `binary_identity`), src/refmatrix/cli.py:2480-2491 (`hub launchctl install`) | plan-4 Q15 built the dev-tree refusal and pointed it at the 8 daemon plists; the same commit edited `render_hub_plist` two lines below the untouched `_rmx_path()` call. Quoted-line-sibling, 4th sighting in this lineage. Worse than the daemon case: a drifted daemon plist is caught by `check(root, rmx=)` on every `relaunch-fleet`; nothing ever compares the hub plist to its render — and the hub is the fleet's watchdog, the owner of the shared model workers, and the version handshake | FIXED as specified. `render_hub_plist(*, port, host, rmx=None, allow_dev=False)` and `install_hub(..., rmx=None, allow_dev=False)` both call `_refuse_dev_tree(rmx)`, and in `install_hub` it runs BEFORE the stop order, the bootout and the write — a refusal that fired later would already have torn down a working hub to reject its replacement. New `check_hub(*, rmx, port, host)` mirrors `check(root, rmx=)` including naming a dev-tree binary as drift that must NOT be 'fixed'. Wired so it is actually read: `hub launchctl install` passes `sys.argv[0]` and gains `--check` / `--allow-dev`, and `hub launchctl status` prints `plist in sync` or the drift reason. `tests/test_hub_plist_devtree.py` (9 tests) incl. the bug-023-shaped assertion that a refused install writes no plist and makes zero launchctl calls; 2 mutations (refusal removed -> 1 fail; render ignoring the caller's binary -> 2 fail); 88 passed across the hub/launchd suites | launchd, hub, dev-tree-refusal, quoted-line-sibling, plan-4 | fixed 2026-09-16 | 1 |

<!--
Entry conventions:
- ID: bug-NNN, zero-padded, monotonic.
- Symptom: the exact error string or user complaint (quote errors verbatim).
- Root cause: WHY it broke, not just where.
- Fix: what changed to resolve it (file:function or the concrete edit).
- Tags: kebab keywords for search (e.g. auth, off-by-one, import, migration).
- Seen: occurrence count; bump + flip Status to `recurring` if it reappears.
-->
| bug-029 | `.venv-eval/bin/rmx` ran `refmatrix.cli:main`, bypassing `cli_entry` | `.venv-eval/bin/rmx` (generated console script) | Stale console script generated before `cli_entry` was introduced; `pyproject` declares `refmatrix.cli:cli_entry` and the deploy venv's script uses it, so only the DEV binary was affected | Rewrote the script to call `cli_entry` (deliberately not a pip reinstall — mixed-ABI tree, 19 phantom failures on record). Old copy at `/tmp/rmx-devscript.stale.bak` | dev-env, telemetry, console-script | fixed 2026-09-15 |
| bug-030 | `TimeoutError: timed out` in `modelsrv.recv_frame` — `rmx embed --kinds memory` died after 30.8s against the hub's shared worker, aborting the LongMemEval build after a SUCCESSFUL 4h23m ingest | `src/refmatrix/modelsrv.py:151-160` (`WorkerClient.call`), caller `src/refmatrix/embedder.py:155` | **DIAGNOSIS CORRECTED 2026-09-16 — the original root cause was wrong and could not produce this symptom.** It blamed `call(timeout=X)` leaking a short timeout onto the persistent socket, called a 2nd sighting of `impression_bsd_probe_timeout_leak`. Four facts against it: (1) that leak was already fixed in `d576a81` (2026-09-15 03:28) with the `finally` restore and `test_a_timed_probe_does_not_shorten_the_next_call`; (2) `PROBE_TIMEOUT_S` is **45** and `SHARED_OP_TIMEOUT_S` was **30**, so a leaked probe timeout would have made the next call LONGER, never shorter; (3) 30.8 s IS the op budget — the daemon builds `SharedWorkerClient(role, timeout=SHARED_OP_TIMEOUT_S)`; (4) `modelsrv.py:61-63` records a cold worker's model load as **14.9 s (embed) and 33 s (rerank)**, so a 30 s work budget is below the cold load written directly above it. REAL CAUSE: the fleet waited 45 s to QUALIFY a worker and then allowed 30 s for the WORK. Both constants had a test (`test_bug015_wedge` pins `PROBE_TIMEOUT_S >= 40` and separately allows the op budget anywhere in 5..60); their RELATIONSHIP had none, so they drifted into contradiction | `SHARED_OP_TIMEOUT_S` default 30 -> 45 and FLOORED at `modelsrv.PROBE_TIMEOUT_S` via `_shared_op_timeout_default()`, so an env override can raise it but never undercut the probe (verified: env=5 -> 45.0, env=120 -> 120.0). bug-015's bound is intact — still far below the 300 s client default. `tests/test_worker_budget_invariant.py` (5 tests) pins the pair and the end-to-end hand-off from the daemon to its client; 2 mutations run (un-floored 30 s -> 3 fail; probe dropped to 20 s -> 1 fail) | model-workers, timeout, benchmark, drifting-constants, wrong-diagnosis | fixed 2026-09-16 |
| bug-031 | A latency figure was taken during a store's own cache construction and reported as steady state — `rmx context` "62.9 s" on the LongMemEval store | `workflow/measurements/longmemeval-latency.md`, `eval/production/longmemeval/REPORT.md` (both corrected) | Measurement at ~20:35-20:40 while the daemon built a 432 MB `adjacency.cache.npz` (mtime 20:50, TEN MINUTES LATER) and replicated three 2.4 GB catalog slots. "Daemon warm" was checked on the reranker only, not the store. Mirror image of `impression_bsd_cost_measured_idle` | Retracted and replaced with a profile: `build_context` warm 0.45 s, BM25 over 406,885 concepts 0.37 s. Rule earned: before quoting a latency taken after any restart or first-run, diff the store's own cache/slot file mtimes against the measurement time — a file written AFTER the measurement means it was taken mid-construction | measurement, performance, self-inflicted | fixed 2026-09-16 |
| bug-032 | Cross-encoder rerank scores only the FIRST 2048 chars of a document; on LongMemEval **36.9% of answer-bearing turns sit beyond that offset** and are invisible to the reranker | `src/refmatrix/reranker.py:43` (`MAX_DOC_CHARS = 2048`), applied at `:138`/`:166` as `(d or "")[:MAX_DOC_CHARS]`. Reached ONLY via `scan.py:947-961` -> `content_only_bundle(reranker=shared_reranker(...))` and by `memory recall` | The bound is sound (~512-token ceiling minus query). The STRATEGY is not: head-truncation assumes "a memory's head (title + lead) is what the cross-encoder needs to rank it" — true for a curated GMD memory, FALSE for a chat transcript with no lead. Measured on longmemeval_oracle (896 answer-bearing turns): median offset 0, p75 3,268, p90 8,117, **331/896 = 36.9% past 2048** | NOT YET FIXED. Proposed: window the doc around query-term hits with the EXISTING `kwic.py` windower instead of `[:2048]`. **ATTRIBUTION CORRECTED (ch-bsd r2):** the first version of this row blamed `single-session-preference` **0.200**, which is the `context` row — and `rmx context` NEVER constructs a reranker (`build_context(reranker=)` left `None`; zero rerank refs in the context CLI path). Truncation can only touch **`scan`, whose single-session-preference is 0.150**, and only if the hub's shared worker answered during the 6,983 s run — which bsd-plan2 r1 says is exactly what usually does NOT happen under load. Verify worker activity in the run window before claiming any contamination | rerank, retrieval-quality, truncation | open 2026-09-16 |
| bug-033 | ~8.5 s unattributed between `build_context` (0.45 s warm) and the full daemonless `rmx context` CLI (9.0 s) on the LongMemEval store; only 2.8 s of it is CPU | `src/refmatrix/cli.py` context path, `src/refmatrix/context.py::build_context` (the 0.45 s core is NOT the cost) | UNKNOWN — not profiled. The retrieval core was profiled and cleared; the gap is everything around it (store open, replica bind, model/worker attach, import cost). Split out of bug-031, which was the RETRACTED 62.9 s storm-window figure and is `fixed`; pointing at bug-031 for this gap told a reader the gap was closed (ch-bsd r4 #m-2-r4) | NOT YET FIXED — this row exists so the gap has an open anchor of its own. Profile the daemonless CLI end to end (`-X importtime` plus a wall-clock split around store bind) before proposing anything | performance, measurement, longmemeval | open 2026-09-16 |
| bug-034 | Five tests fail on `master` on an idle machine, in every full-suite run: `test_bigop_nonblocking::test_pause_blocks_restart_of_a_dead_daemon`, `test_daemon_liveness::test_daemon_status_reports_busy_for_a_live_unresponsive_process`, `test_graph_landing::test_context_op_honors_partition_under_ambient_drift`, `test_hub::test_watchdog_restarts_down_daemon_when_auto`, `test_memory_recall_exclude_mtype::test_scope_both_filters_global_rows` | the five test modules above; four are daemon/hub/liveness, plan-4's open territory | NOT INVESTIGATED. Reproduced identically in `pytest-post-bsd-r2.log` (6 failed incl. one since attributed) and `pytest-head-05bef34.log` (5 failed, idle machine, 837 s). `test_graph_landing` is the perma-red named in plan-6 task 6.4 | FIXED. All five fail in ISOLATION in under 0.3 s, so none was contention. Four distinct causes, and **not one was a product bug** — every one was a test left behind by a deliberate change, so four real contracts had no passing guard: (1)+(4) `test_pause_blocks_restart_of_a_dead_daemon` / `test_watchdog_restarts_down_daemon_when_auto` ticked the watchdog ONCE after plan-4 r3 #s-3 made the no-process branch kick on the SECOND consecutive tick; both now assert the first tick only arms the counter. (2) `test_daemon_status_reports_busy_for_a_live_unresponsive_process` patched `_read_pid` but not `pid_is_rmx`, and under pytest the command line is `python -m pytest`, which correctly does not match — fixture wrong, guard right; added the missing negative (a foreign process reusing the pid number is NOT busy). (3) `test_context_op_honors_partition_under_ambient_drift` — the plan-6 task 6.4 'perma-red' was a hand-rolled `class D` pinning `d.store` after the op moved to `d._st()`; `AttributeError`, never a partition bug. (5) `test_scope_both_filters_global_rows` — the `_global_recall_rows` fake named every parameter and died with `TypeError` when the CLI added the `timeout`/`retries` ping budget; now `**kw`. Four mutations run, each killing its test: first-tick kick (2 fail), `busy` ignoring `pid_is_rmx` (2), dropping the explicit partition bind (1), `mt_excluded` always False (5) | tests, daemon, hub, stale-fakes | fixed 2026-09-16 |
| bug-035 | `inspect.getsource` assertions fail with a content mismatch against a line from an UNRELATED function when `src/` is edited while the suite runs — surfaced as the phantom 6th failure `test_plan4_remedy_r2::test_foreground_start_refuses_a_busy_daemon_without_reaping_it` in `pytest-post-bsd-r2.log` | `tests/test_plan4_remedy_r2.py:263`, `tests/test_brief_surfaces.py:137` | `linecache.checkcache` re-reads the edited file and slices it with the ALREADY-LOADED code object's `co_firstlineno`. Commit `6b6a610` added +5 lines at `daemon.py:4305` at 01:00:00; that suite started ~00:48 and finished 01:02, so the file moved under it. The failure is silent about its cause, which is why it cost a handoff section and a round of audit time to attribute | Replaced both call sites with `tests/conftest.py::source_of(mod, name)`, which reads the module from disk and slices by NAME — an edit elsewhere in the file cannot shift it. Guard test `test_no_test_reinterprets_a_loaded_code_object_against_an_edited_file` fails if any test reintroduces `inspect.getsource(` (ch-bsd r4 #m-4-r4) | tests, hermeticity, self-inflicted | fixed 2026-09-16 |
| bug-036 | `tools/gmd/lint.py` and `src/refmatrix/ingest_gmd.py` disagree about what a `{#anchor}` IS. Lint makes any end-of-line `{#id}` addressable; `parse_gmd` builds a node ONLY for a heading. In `workflow/bullshit/IMPRESSIONS.md`, **71 of 73 anchors are invisible to the store** — every per-run `{#imp-*}` on a list item, all pre-existing | `tools/gmd/lint.py:165-180` (`ID_RE.finditer` over every body line) vs `src/refmatrix/ingest_gmd.py:349-375` (node created inside `if hm:` — the heading branch only) | Two parsers, one syntax, never compared. GMD's own primer says an anchor is "a stable node id for any heading, paragraph, or list item"; the lint implements that, the ingest does not, so a `rel:` edge to a list-item anchor lints clean and resolves to nothing in the graph. Same shape as ch-bsd r4 #b-2-r4: a gate agreeing with the author while the real consumer sees something else | NOT YET FIXED. Found while repairing the 97 dangling-anchor errors (ch-bsd r4 remedy): the repair makes the LINT green — which is the stated `CLAUDE.md#before-commit` gate — and does NOT make those ids resolvable in the store. `parse_gmd` now emits a node for a body `{#id}` (paragraph or list item), parented to its enclosing heading, never shadowing an existing id, and still skipped inside code fences and inline code. `rel:` ATTACHMENT IS DELIBERATELY UNCHANGED: the spec would attach an edge to the nearest enclosing block with an id, but measured over docs/ workflow/ eval/ CLAUDE.md that moves 32 of 599 existing edges (5.3%) to a different owner — resolvability is reversible, re-pointing live edges is not, so that is its own decision. `tests/test_gmd_body_anchors.py` (11 tests) asserts the two parsers agree on IMPRESSIONS.md itself | gmd, lint, ingest, two-parsers, graph | fixed 2026-09-16 |
| bug-037 | A full-suite run reported 2 spurious failures (`test_shared_probe_covers_a_cold_worker`, `test_the_probe_budget_still_covers_a_cold_load`): `PROBE_TIMEOUT_S` read **20.0** at runtime while the source on disk read **45**, same file, same interpreter | `src/refmatrix/__pycache__/modelsrv.cpython-314.pyc` (stale), source `src/refmatrix/modelsrv.py:68` | A mutation-check edited `"45"` -> `"20"` — the SAME byte length — and the source was then restored from a backup. The `.pyc` written from the mutated source survived the restore and kept being served; `python -B` does not help (it skips WRITING bytecode, not reading it). `rm -rf src/**/__pycache__` restored 45.0 immediately. Exact invalidation mechanism NOT confirmed — Python keys a pyc on the source's (mtime, size) and a same-length edit removes size as a discriminator, but the surviving mtime match was not proven, so the cause is recorded as observed rather than explained | FIXED (task 12.5). `tools/pyc_guard.py` + a `pytest_sessionstart` guard in `tests/conftest.py`: every `.pyc` under `src/refmatrix` is recompiled as PEP 552 `checked-hash` before a single test runs, so validation is by SOURCE HASH and no combination of mtime and byte length can fake it. `unchecked-hash` is rejected too (it skips validation entirely). The guard prints what it recompiled — a silent repair between source and test result is the same class of failure as a silent skip on a memory path. `RMX_PYC_GUARD=0` opts out; `python3 tools/pyc_guard.py <root>` runs it by hand after a mutation batch. The mechanism is REPRODUCED in `tests/test_pyc_invalidation.py` (same-length edit + a restore onto the recorded mtime serves the stale constant) and the test fails loud if it ever stops reproducing, rather than passing vacuously. Measured on the dev tree: 12 timestamp pycs converted on the first run | tests, bytecode, mutation-testing, self-inflicted | fixed 2026-09-16 |
| bug-038 | The version had two sources of truth and they disagreed for a whole release: `src/refmatrix/__init__.py` read **0.70.0** while `pyproject.toml` read **0.69.1** | `src/refmatrix/__init__.py:2`, `pyproject.toml:7` | The 0.69.1 -> 0.70.0 bump edited `__init__.py` only. Nothing asserted the two agree. The runtime reads `refmatrix.__version__` (`upgrade.runtime_identity:130`, `hub._HUB_IDENTITY:1141`), so the hub version handshake, `rmx version` and the fleet staleness check all reported 0.70.0 while the installed dist-info recorded 0.69.1 — the exact gap `reference_editable_venv_distinfo_lag` says "which code is running" arguments start in | Both synced to **0.71.0** (this deploy carries the r4 remedy plus bug-008/028/030/034/036, materially more than 0.70.0 described). `tests/test_version_single_source.py` asserts they agree, so a one-file bump now fails the suite | version, deploy, two-sources-of-truth | fixed 2026-09-16 |
| bug-039 | This project's own store was structurally UNDER-DERIVED while every health surface read green. 7 of 12 fixed scan-prompt prompts sat at a 1-bundle / 10-node floor; a `reingest --force` took the set to 2.0x bundles, 2.3x nodes, **2.46x tokens** (952 -> 2,346 mean per prompt) | `.refmatrix/` (refmatrix's own store); no detector | Not force-re-derived since 0.49.1 — ten days and several ingest changes. The ONLY visible signal was `stale_files: 35`, which reads as routine log churn and was dismissed as exactly that three times in one session while being looked at directly. `daemon_up`, `dev_tree: false`, version parity and `rmx install-hooks --check` were all green throughout. Same shape as `project_cliquet_catastrophic_repair` (green health, dead store), one severity down | FIXED (task 12.4). New `derive_stamps(partition_id, pass_name, version, derived_at)` table in both CATALOG_DDLs (created on connect, no migration step). `Store.stamp_derive` is called at the END of `ingest_path` (outside the transaction, so a crashed ingest leaves the previous stamp standing) and at the end of `ingest_gmd_paths`; `Store.derive_status()` returns `{passes, oldest_version, running_version, stale, reason}`. Two deliberate rules: an UNSTAMPED partition that holds tracked files reads STALE (that is exactly the state the incident hid in — reporting it as unknown would preserve the blind spot), and the version comparison is exact inequality, so a rolled-back deploy is flagged too. Surfaced on `_store_health` (what the hub alert tick reads) and by `rmx daemon status`, which prints `derive: 0.49.1 (running 0.71.0) — stale; re-derive with rmx reingest --force` and prints NOTHING when current. Verified live on a throwaway store: current after a real ingest, stale line rendered after stamping 0.49.1 | store-decay, detection-gap, scan-prompt, context-cost | fixed 2026-09-16 |
