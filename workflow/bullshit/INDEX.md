# ch-bsd ledger — refmatrix

| hash | date | author | verdict | findings | summary |
|------|------|--------|---------|----------|---------|
| f56a365 (69a1b37..f56a365) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 15 (5B/5S/3M) | [[bsd-0661-e2e-memory-bridge]] — bridge skips non-GMD memories silently; deploy venv imports dev tree; grep-learn crash-loops daemon on drifted PK index; installed hooks unreproducible + PreCompact silences bridge failure; stale deferrals (ingest_gmd/store/duckdb_view/embedder) |
| cabce24 (cf4da79..cabce24) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 9 (2B/4S/3M) | [[bsd-plan1-deploy-runtime-cabce24]] — #bs-2 environment closed (deploy venv → ~/refmatrix/src, 7/8 daemons verified); but `global:queues` alert never gates on dev_tree; `upgrade()` verify skipped on "already up to date" and checks root/.venv of the imported tree; hub status renders unknown as clean (orderly v0.65.0); hub's own tree unreported; surface tests bypass wiring, mocks unregistered; plan flipped to completed before the bsd gate |
| 03f46b8 (cabce24..03f46b8) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 11 (3B/4S/4M) | [[bsd-plan2-hooks-reproducible-03f46b8]] — #bs-4/#sk-5 closed by render (installed == generated, memory paths loud, opt-outs reachable); but `--enforce` forced-on is a dead flag; `--force` still deletes hand-authored `enforce-*.sh` hooks (prefix signature); task 2.3 docs dropped (README/INTEGRATION name settings.local.json); check() blind to duplicates/unmarked/extra-key drift; plan flipped completed before gate (2nd); Stop promote exceeds "equals today" requirement; `--detach` guard calls a busy daemon "no daemon" (seen live) |
| 6cc33e9 (cabce24..6cc33e9, plan-1 r2) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 5 (1B/1S/3M) | [[bsd-plan1-deploy-runtime-r2-6cc33e9]] — #b-1 alert gate, #b-2(a) upgrade identity-first, #s-3/#s-4 hub surfaces (live), #s-5, #s-8, #m-7, #m-9 closed; modelsrv 300 s wedge fixed; OPEN: `daemon restart --relaunch` compares CLI vs daemon code path but both share ~/bin/rmx, so the incident state shows "relaunch verified"; ping failure also passes; alert publish untested; unknown-identity rows not hot; memory lint 6 dangling ADR links, bridge pending |
| 6cc33e9 (03f46b8..6cc33e9, plan-2 r2) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 7 (0B/3S/4M) | [[bsd-plan2-hooks-reproducible-r2-6cc33e9]] — #b-1/#b-2/#b-3/#s-4/#s-5/#s-6/#s-11/#m-8/#m-10 closed by render+probe (forced enforce emits 4; same-prefix user hook survives --force; check() catches timeout/duplicate/whitespace/move/matcher + script drift; busy≠absent); OPEN: Stop promote cost 55 s/15 s/6.6 s live vs Q4's 0.13 s; `--no-claude --apply` then `--check` = 17 lines false drift (`claude` flag unrecorded); intuition doc Stop section still deferred to nonexistent "Phase C4"; refusal names missing `--no-promote`; busy branch untested + 2x wait accounting; 3 hook cmds unobserved (user-gated) |
| 8ba4799 (03f46b8..8ba4799) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 11 (3B/4S/4M) | [[bsd-plan3-verbs-parity-8ba4799]] — tools==verbs real (28/28 deployed 0.68.1, direct-call handlers gone, widen mutation fails, table output byte-identical); but "CLI calls the verb" is an existence check (26/28 twins bypass verbs), parity test excludes every memory_recall param (MCP k=8 vs CLI k=10 hidden), task 3.2 shipped 18 verbs with zero tests + no TDD record; dense path + daemon-down fallback remain a second "which rows"; CLI --recent vs MCP recent=true differ on session/*; PATTERN filed [[bsd-pattern-tests-prove-existence-not-wiring]] |
| e6c4081 (6cc33e9..e6c4081, plan-1 r3) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 4 (0B/1S/3M) | [[bsd-plan1-deploy-runtime-r3-e6c4081]] — #b-1-r2/#s-2-r2/#m-3-r2/#m-4-r2 closed; relaunch guard fires on dev_tree either side (incident replay + live relaunch at e6c4081); bridge done, migrated memories resolve; residue: FakeBus/FakeHub unregistered, memory lint 3 cross-tree ADR links, ping-exception branch untested, `identity_error` unread by any consumer. No blocker. |
| c9d75af (e6c4081..c9d75af, plan-2 r3) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 8 (1B/3S/4M) | [[bsd-plan2-hooks-reproducible-r3-c9d75af]] — r2 #s-1..#m-7 closed as stated (retries=0 passthrough real, 5 s bound holds for memory_add, --no-claude check clean, Phase C4 gone, deploy==master); but the bound covers 1 of the command's 3 daemon calls: subject-active Stop hook measured 180.2 s; busy daemon called "not running" by bare ping; --detach busy path 7.4 s for a 1 s budget with the test patching the two functions that carry it; daemon_status mock unregistered; PreCompact promote unbounded |
| 54fceb9 (54fceb9^..54fceb9, plan-1 r4) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 4 (1B/1S/2M) | [[bsd-plan1-deploy-runtime-r4-54fceb9]] — #s-1-r3/#m-2-r3/#m-3-r3 closed; 21:18 alert carries orderly `identity: unknown`; but #m-4-r3 remedy is dead on the wire: `_op_ping` never sends `identity_error`, all 3 new readers + 4 tests fake the ping, hub's own error still unread by `hub status` (escalated to BULLSHIT, 3rd tests-bypass-wiring in plan 1); `hub queues` raw TimeoutError traceback under a busy daemon; plan-of-plans/plan status disagree; plan flipped completed on a remedy that cannot run — revert |
| d68856d (c9d75af..d68856d, plan-2 r4) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 5 (0B/2S/3M) | [[bsd-plan2-hooks-reproducible-r4-d68856d]] — r3 #b-1/#s-2/#s-3/#s-4/#m-5/#m-6/#m-7 closed and reproduced against a real stalled socket (subject filing 0.51 s bounded, busy names pid, detach 1.27 s for 1 s budget, --no-claude reaps, PreCompact --timeout 30 live); OPEN: SessionStart bridge hook bounded only when silent — `partition_list` 10 s×3 behind the writer lock, 210 s + blank traceback with bg pool saturated; `filed_subject_error` written, read by nothing; per-call additive budget; #m-8 user-gated. No blocker — plan may close with the two SKETCHY fixed or justified. |
| 399d0a3 (b252a09..399d0a3, plans 7-10 r1) | 2026-09-16 | Todd Holley / Opus 5 (1M) / orchestrator | DIRTY | 19 (8B/7S/4M) | [[bsd-plan7-10-r1-399d0a3]] — wiring is real end to end on a spawned daemon (verb→op→CLI, `--save` writes ids 7/8 recallable and idempotent, derivation on `_read_with_fallback` with the slot untouched, `CountingStream` byte-exact across 8 renderers and on the deploy script, uncounted honest on the live 32,364-row log, task 8.4 left no stub, REPORT.md does not overclaim); OPEN: `memory brief --gmd` dies `AttributeError: 'dict' has no attribute 'cls'` on its first real run (op serialises, renderer expects objects; no test invokes the flag — plan-3 r6 #b-1 one plan later); `contradicted` cannot fire on the production corpus (41 pairs, 54/59 endpoints are `#anchor` concepts, 0 emitted / 41 skipped); `orphan_concept` skips `terms.STOPWORDS` so the top live brief is `the` (6th call site of the bug its own docstring cites); `brief/*` unexcluded so saved briefs return as `singleton` findings about themselves (reproduced); the GMD round-trip test hardcodes the `evidence` `parse_gmd` cannot recover; `--depth` inert for `scan`/`scan-nocontent` while `_meta` records 50 and REPORT's next step is `--depth 200`; 88.2% of the "per-prompt hook budget" windows contain no prompt (10,740 windows, 1,271 with scan-prompt, 75% of hook rows are PostToolUse focus); mock registry untouched (5th strike). Plans 7/8/9 may NOT flip completed; plan-10 already did, on a gitignored artifact. |
| a249877 (399d0a3..a249877, plans 7-10 r2) | 2026-09-16 | Todd Holley / Opus 5 (1M) / orchestrator | DIRTY | 5 (2B/1S/2M) | [[bsd-plan7-10-r2-a249877]] — 11 of 13 closed and five proven by MUTATION (reverting the `--gmd` and `brief/*` fixes each fails a test in `test_brief_bsd_r1.py`); `the` gone from the live replica with `STOPWORDS is terms.STOPWORDS`; round trip real, ids in the render, doc lints 0; #s-13 and #s-15 done despite being declared not-attempted — authored-date lifts distinct days 13 → 39 on production data; #b-7 replayed over 23,662 real hook rows: 10,802 → 1,273 windows, p50 0 → 4,370 B, 3.0% contamination (the claimed '0 windows' was vacuous — `counted==0` on the live log). OPEN: `contradicted` went silent → 47 emitted of which 45 are ch-bsd's OWN `rel: contradicts` edges, 0 have both endpoints as memory rows, the finding line still says 'two memories', the `--gmd` render calls ids unresolved two lines after naming them, and the mtype exclusion the docstring claims is bypassed by the `get_entity_by_id` fallthrough (90 excluded rows reachable); `--max-tokens` is INERT on `--format json` (budget 1 vs 100,000 → 11,931 B both; text mode 2,754 → 6,059 B) so depth is still dead for `scan`, one flag over, and the new gate asserts argv not behaviour; `test_brief_real_daemon.py` passes under BOTH mutations (fixtures yield 0 briefs because `memory add` writes no fragments) while the registry marks three fakes GRADUATED on it; `log_cli_intent` still writes the live cli.log (280 → 281, bisected); bug-032 blames the 0.200 `context` row, which never constructs a reranker — only `scan`/0.150 reaches the cross-encoder. |
| 4f451c5 (a249877..4f451c5, plans 7-10 r3) | 2026-09-16 (artifacts written 00:53; the `0105` in the filename and the `01:05:00` in last_run.log are both forward-dated — ch-bsd r4 #m-1-r4) | Todd Holley / Opus 5 (1M) / orchestrator | DIRTY | 5 (1B/1S/3M) | [[bsd-plan7-10-r3-4f451c5]] — all five r2 findings closed and the detector work finished: `contradicted` 51 pairs → 2 emitted / 49 skipped with every one of the 49 enumerated and correct (all 20 one-sided drops are a finding citing the rule it violates; `adr-` passes the regex), mtype fallthrough refuses excluded rows, finding says 'two nodes', Mutation A now KILLS the seeded real-daemon test, `DEPTH_UNCONTROLLED` is the honest call. OPEN #b-1-r3: the r2 names-map fix is destroyed nine lines later — `console.print` parses `[[note-1]]` as rich markup, so `--gmd` ships `Evidence (2): [] `1`` and 8 × `rel: evidence-for -> []`, an edgeless GMD doc on which `tools/gmd/lint.py` reports 0 errors (the commit gate cannot see it); same defect at `cli.py:11164` for `memory compile --out -`; invisible because the CliRunner test's fake omits `names` (no wikilink to mangle) while the test that asserts `[[lonely]]` never goes through the CLI. Also: REPORT.md says b-6 'was fixed' when this commit deliberately declined to fix it, and `#saturation` still frames both rows as depth-50; the skip string blames 'unresolvable ids' for 49/49 process-authored drops (3rd wrong wording); `log_cli_intent` patched in the file the finding named, not in the two that invoke the command. Attack answers: regex NOT over-filtering, second read ~1 ms but doubles writer-lock exposure in the boot window, and the 'honest gap' IS closable — poll `memory list` through the daemon, verified to pass (6→6) and to fail under the mutation (6→8). |
| 05bef34 (4f451c5..05bef34, plans 7-10 r4) | 2026-09-16 | Todd Holley / Opus 5 (1M) / orchestrator | DIRTY | 10 (2B/4S/4M) | [[bsd-plan7-10-r4-05bef34]] — best remediation in this lineage: all four r3 code findings closed and proven by MUTATION (M1 `console.print` revert fails 3 tests incl. the real-daemon one, stdout byte-identical to the r3 finding; M4 two-read revert fails the read-path test; M5 `brief/*` revert fails the new live poll-the-daemon test — the gap r3 called unclosable), and `#m-5-r3` proven behaviourally (live cli.log 33443 → 33443, delta 0). `--gmd` verified end to end on a real seeded daemon: every wikilink and every `rel: evidence-for` edge survives. The sixth failure is ATTRIBUTED and reproduced byte-for-byte: `inspect.getsource(serve_foreground)` re-read a `daemon.py` edited mid-suite (+5 lines at :4305, commit at 01:00:00, run started ~00:48) and sliced the NEW file with the OLD `co_firstlineno` — not a flake, not the version bump. OPEN #b-1-r4: `render_gmd(stats=)` has NO production caller, so every real `--gmd` doc says 'an unrecorded number of memories' while `--json` and the table print 243 / 8 — the r2 `#b-2-r2d` loud-fallback-at-100% shape, one argument over in the same call, and a dead parameter. OPEN #b-2-r4: the `#s-2-r3` REPORT.md edit left an UNTERMINATED fence (count 2 → 3), so the project's own `ingest_gmd.parse_gmd` loses the `#next` node (12 ids → 11) — and `eval/` is not in `scripts/lint-gmd.sh` SCOPE, so no gate lints that doc at all. Also: the `memory compile --out -` sibling fix is unguarded (M2: 95 passed; NO test invokes `memory compile` through the CLI); `skipped_by` omits singleton skips so the CLI prints a literal `(?)`; M3 deletes the only bucket that fires on the live corpus (51/51 `workflow_authored`) with 87 passed while the one tested bucket is 0 in production; registry omission (6th). Plans 7/8/9/10 correctly stay in-progress. Fleet untouched: daemon 16448, hub 13847, 8 stores, hooks in sync. |

## Leaderboard (updated every 10 runs) {#leaderboard}

| Author | Commits | BULLSHIT | SKETCHY | MEH | BS Rate |
|--------|---------|----------|---------|-----|---------|
| Todd Holley / Fable 5.1 / orchestrator | 28 | 47 | 60 | 69 | 1.68 |
| Todd Holley / Opus 5 (1M) / orchestrator | 7 | 18 | 23 | 20 | 2.57 |

Runs 1-10 (2026-09-14): BULLSHIT per run 5,2,3,1,0,3,0,1,1,0 — first-round audits of a plan average 3.3 B; re-review rounds average 0.6 B. The remaining B-rate is concentrated in first drafts, not remedies.

Runs 11-25 (2026-09-14 → 2026-09-15): BULLSHIT per run 2,2,1,2,6,2,2,1,4,2,0,0,1,2,1 — every run but two is a remedy re-review; the two first-round audits in the window (plan-4 r1: 6, plan-5 r1: 2) still lead, and re-review rounds now average ~1.8 B, up from 0.6 in runs 1-10. Every re-review B is one of three shapes: busy≠absent one call over, a bound on the surface the finding quoted and not its sibling, and a claim of "every X" that a grep of the file disproves.

Runs 26-28 (2026-09-15): BULLSHIT per run 1,1,1 — all three are late remedy re-reviews (plan-3 r5, plan-4 r4, plan-2 r9) and each single B is the SAME leg the previous round blocked on, re-measured and still dead. The re-review B-rate has stopped falling at 1.0: what survives is never a missing fix, it is an acceptance criterion that measures wall time, an artifact nobody re-renders, or a number taken in the quiet window after a restart.

Run 29 (2026-09-16, plans 7-10 r1) — 8 BULLSHIT, the highest first-round count in the ledger and the first run under a new author profile. Shape has shifted: none of the eight is a busy≠absent or a partial bound. Six are a claim the module's OWN docstring makes about itself and the code does not keep (stoplist imported except the one that matters, exclusions applied to everything but its own output, a round trip that recovers everything but the field it calls "the whole contract", a depth knob recorded in the artifact and dropped from the argv, a "per-prompt" budget whose population is 88% not-prompts, and a flag no test invokes). The re-review lineage taught remedies to survive my probes; a FIRST draft in a new problem domain reverts to the 2026-09-14 baseline — and the docstrings are now so good that they are the fastest place to find the lie.

| 209270d (8ba4799..209270d, plan-3 r2) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 5 (2B/2S/1M) | [[bsd-plan3-verbs-parity-r2-209270d]] — all 11 r1 findings closed for real (wiring recorded, 58 pairs compared incl. 11 recall, spawned daemon + real bus, bug-007 fix proven by mutation, 136 + 1528 passed at HEAD); OPEN: `_hub_rpc`/`queues` swallow `ok:false` (`rmx bus pub` regressed to bare KeyError; hub timeout = traceback); busy daemon still "not running" in `verbs._call`/`federated_locate` — 3rd sighting, escalated; gate proves call not render; full-suite log ≠ committed tree (2nd) |
| 7b35e80 (54fceb9..7b35e80, plan-1 r5) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 3 (2B/0S/1M) | [[bsd-plan1-deploy-runtime-r5-7b35e80]] — all four r4 findings closed and reproduced (`_op_ping` forwards `identity_error`, producer-side test fails under my mutation; `hub status` renders the hub's own error; `queues` on the typed `_hub_rpc` boundary, per-root cap, live 21 s run succeeded on a restarting fleet; all-five RED; both status files in-progress; live rewriter hook = deploy render); OPEN: third unregistered-mock strike (`hub.status`/`hub.rpc`/`discovery.*` in test_plan1_remedy.py, escalated to B); bug-008 deferred to "task 6.4 (or a new 6.5)" — 6.4 does not cover it (rule 9a, B); dead `except` in `hub_queues`. Plan may flip completed in the commit that adds the rows + a task 6.5 spec. |
| a89c733 (d68856d..a89c733, plan-2 r5) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 5 (1B/1S/3M) | [[bsd-plan2-hooks-reproducible-r5-a89c733]] — all five r4 findings closed and reproduced on a real socket (deadline holds to 0.01 s, refusal wording, `filed_subject_error` reaches CLI+MCP, Stop `--timeout 5` seen holding live at 5.04 s/exit 1); BULLSHIT #b-1: the UserPromptSubmit `memory recall --stdin-json` hook is unbounded and holds every prompt 20 s at the median (36 rows, re-measured 19.6 s) — 3rd partial-bound sighting → PATTERN file; `completed` does NOT stand |
| 76929a2 (209270d..76929a2 + 0036d80, plan-3 r3) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 5 (2B/2S/1M) | [[bsd-plan3-verbs-parity-r3-76929a2]] — r2 #b-1/#b-2/#s-3/#s-4/#m-5 closed at the quoted lines and reproduced (real-socket hub boundary, typed busy seen live on pid 30867, gate binds+canary, logs recorded); OPEN: `rmx_save_state` → `handoff._promote_digest` opens `catalog.duckdb` under a busy daemon (4th busy≠absent, write path); `skipped` leg unread by `rmx locate` (live `no matches` on the busy store) + `federated_query` never got one (3rd unread-field); two bare pings left in verbs.py vs Q11; op-timeout-is-busy untested (mutation D passes 82); read twins refuse on busy at 3× classification cost |
| 6ae4b6f (d68856d..6ae4b6f + 190a9d1, 23549ae; plan-4 r1) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 12 (6B/2S/4M) | [[bsd-plan4-daemon-resilience-r1-6ae4b6f]] — gates real where they run (boot repair via real boot, busy-heartbeat live ×8 with 0 restarts, orderly adopted + runs static 30 min, Q3 accepted); but RepairAbort returned as ok:false INSIDE ok:true → `repair-index --entities` green on abort; adoption pings bare 0.5 s → busy predecessor SIGKILLed in 3.5 s (4th busy≠absent, PATTERN filed) + offline writer open sibling; kill grace spent BEFORE the signal (stop-op branch unreachable from the watchdog, SIGTERM→SIGKILL 2 s); marker written by 1 of 8 detectors (sync-path loop unfixed, 1 line); sqlite branch cascade-wipes concepts/memories/links; 5/8 fleet daemons still 0.66.3, no heartbeat, `hub status` blind to version; heartbeat ≠ serving (deadlock = busy forever); mocks unregistered (4.1/4.2/4.4) |
| 1eea8ed (191e510..1eea8ed, plan-5 r1) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 9 (2B/5S/2M) | [[bsd-plan5-memory-bridge-1eea8ed]] — stem id rule, lenient parse, unparseable counter, wait path and real-daemon tests are real; but the bridge under a busy daemon opens the writer slot in-process (`_ingest_gmd_sync`/`_store` bare ping): four live runs 22:30–22:34 logged no daemon job, wrote `MEMORY` ×3 to catalog.B, daemon fast-exited 22:34:45 on a corrupted ART index (4th busy≠absent, 5th index drift); MEMORY.md ingested as a curated memory (live row 3171951); wait path takes a non-memory job as its report (probe: success, 0 rows); `sync-disk --mtype` dead; `skipped_non_gmd` constant on the bridge path (mutation: 37 passed); alias deferred to 0.68 on a 0.69.1 deploy; `waited` unread; parser misfiles unresolved refs; todo G4 stale. Plan may NOT flip completed. |
| 90182c2 (768f868..90182c2, plan-5 r2) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 5 (2B/2S/1M) | [[bsd-plan5-memory-bridge-r2-90182c2]] — all nine r1 findings closed and proven (silent socket refused in 3.9 s with no slot open, MEMORY gone live, 23:19 bridge ran as a daemon job with 160 progress lines, coverage + resume-gate guards fail under mutations C/D, 12 passed at HEAD); OPEN: `daemon_status` calls a booting daemon (live pid, socket not yet bound) ABSENT so `_store(write=True)`/`_ingest_gmd_sync` open the slot in the ~3 s boot window plan 4 opens on every relaunch — the `Could not set lock on catalog.B` boots in daemon.stderr.log are that race (5th busy≠absent); deleting `memory sync-disk` broke the SessionStart guardrail compile (`compile_guardrails.py` → `No such command`, hidden by `|| true`) in this project + 8 fleet projects; dead `memory_sync_disk` body left decorated; silent-socket test's "no catalog file" assertion vacuous on a catalog-less fixture. Plan may NOT flip completed. |
| 3fa98bc (1eea8ed..3fa98bc, plan-2 r6) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 6 (1B/1S/4M) | [[bsd-plan2-hooks-reproducible-r6-3fa98bc]] — r5 #s-2/#m-3/#m-4 closed and reproduced on a real socket, PreCompact now seen bounded live (139 ms); OPEN #b-1: the per-prompt hook with `--timeout 5` deployed = 12.8 s idle (results, no warning), 26.8 s / 18.9 s on its first two live firings — the deadline covers the daemon RPCs, not the rerank on the shared worker (~12.5 s, 300 s socket), the dense path's global leg (90 s at `--timeout 1`), or the startup probe (wall = 2 × budget); 4th partial-bound → PATTERN bumped; tests scoped/toleranced to miss all three; PreCompact recall hook unbudgeted; MCP `timeout` None = unbounded vs CLI 60 s; registry names a file with no patch. Plan may NOT flip completed. |
| e1dba80 (90182c2..e1dba80, plan-4 r2) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 8 (4B/1S/3M) | [[bsd-plan4-daemon-resilience-r2-e1dba80]] — all 12 r1 findings closed at the quoted lines and reproduced (abort `ok:false` on the real wire, SIGSTOPped predecessor survives adoption, `graceful_stop` signals first, marker from every detector, sqlite refused, fleet 8/8 v0.69.1 + versions in `hub status`, thiquet adopted live 23:20:49); OPEN: `RMX_SUPERVISED` gate reaches 6/8 plists — refmatrix + global run without it and a supervised start exits 1 in 0 s (KeepAlive loop reintroduced; reproduced on tmp); serving-derived heartbeat = cli-queue latency (4 in-flight cli ops → age 61 s while 12 bg ops served → watchdog leaves the busy branch; feeds reap/SIGKILL-withhold); `stop_daemon` still grace-before-signal (+4.99 s for 3 s; adoption 20 s; 4th quoted-line sibling → escalated); new bare `ping` gate in `serve_foreground`; kickstart -k at 30 s vs 3×10 s drain; wire test + registry files + task-4.1 spec stale. Plan may NOT flip completed. |
| 768f868 (3fa98bc..768f868, plan-3 r4) | 2026-09-14 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 7 (2B/3S/2M) | [[bsd-plan3-verbs-parity-r4-768f868]] — all five r3 findings closed and each held under my own scratch mutation (M1–M10; no slot opens under silent OR held-writer busy on the promote); OPEN: `rmx_recall_state` still bare-pings `handoff.py:494` → busy = `stale pid` + anomaly, with a dead `busy` render branch since 0.41.0 (5th busy≠absent in lineage); the read fallthrough costs 390 s on a `_PingOnlyDaemon` (`memory get`) and 210 s for MCP `memory_recall` (partial-bound 5th → B); `_promote_digest` bare call 90 s → `TimeoutError` wording; `memory_partition` guesses on op-timeout; r3 test patches unregistered; read dies with the writer's message when no replica; gate cans `skipped: []`. Plan may NOT flip completed. |
| a965805 (90182c2..a965805, remedy 337b709, judged at 7bca4bb; plan-5 r3) | 2026-09-15 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 2 (0B/0S/2M) | [[bsd-plan5-memory-bridge-r3-a965805]] — both r2 blockers closed and proven where they live (real `serve_forever` boot busy at 0.18 s with no socket, slot untouched; installed SessionStart hook run verbatim seeds guardrails through the daemon in 3.1 s and changed the compiled policy on its first run since the alias returned; 51 passed at HEAD; hooks in sync; fleet 8/8 v0.69.1); #s-3/#s-4/#m-5 closed; residue: `pid_is_rmx` PermissionError branch says absent (unreachable per-user launchd), todo.md G4 cell stale. No blocker — CLEAN on a closing commit with exactly those two edits. Outside plan-5: 22 leaked tmp-root test daemons (test_verbs_migrated / test_plan4_remedy fixtures). |
| a6155c6 (closing commit for plan-5 r3, judged at 5b75436) | 2026-09-15 | Todd Holley / Fable 5.1 / orchestrator | CLEAN | 0 | [[bsd-plan5-memory-bridge-r3-a965805#closing-clean]] — diff is exactly the two r3 MEH edits + test + status flips: `pid_is_rmx` PermissionError falls through to `ps` (test both directions), todo G4 cell current; 13 passed at HEAD; plan-5 `completed` stands. |
| 8d0a665 (3fa98bc..8d0a665, plan-2 r7; judged at 7bca4bb) | 2026-09-15 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 3 (1B/1S/1M) | [[bsd-plan2-hooks-reproducible-r7-8d0a665]] — the bound holds on the wire (12 live runs 1.86–4.66 s at `--timeout 5`, results, exit 0; cli.log max 5163 ms) and r6 #s-2/#m-3/#m-4/#m-5/#m-6 closed; OPEN #b-1: the rerank leg is dead as deployed — `info(timeout=1)` leaves the socket at 1 s and `score` inherits it, a healthy worker (25 docs 1.0 s) reranks 0/12 hooks, abandoned requests starve the next probe, same shape in scan.py (bug-015) + daemon's 30 s is 45 s; 3rd plan-2 registry gap (S); `ensure_global_daemon` spawn wait outside the budget (M). Plan may NOT flip completed; fix is 3 lines in `SharedWorkerClient.call` + a real-socket test. |
| ef4365a (e1dba80..ef4365a + 4d31e24, 72f61e0, f733f6c; plan-4 r3) | 2026-09-15 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 7 (2B/2S/3M) | [[bsd-plan4-daemon-resilience-r3-ef4365a]] — all 8 r2 findings closed and proven live (XPC_SERVICE_NAME exported by launchd, 8/8 plists carry RMX_SUPERVISED, `install --check` in sync on both drifters, completion-tick heartbeat 0–5 s fleet-wide, one graceful_stop signalling at t=0 in 3 live restarts, typed foreground gate, socketpair dispatcher test, bug-013 bootout wait); OPEN: the deploy step (`relaunch-fleet` → `restart --relaunch` → bare `kickstart -k`) runs under launchd's `exit timeout = 5` — five daemons SIGKILLed mid-drain tonight in launchd's own log, plist sets no ExitTimeOut, `_verify_relaunch` kill -9s a ping-answering daemon; `_await_shutdown` (#s-5 remedy) unreachable by arithmetic (restart needs hb>60, wait needs hb<45) and by shutdown order (`_stop_heartbeat` first), test patches `heartbeat_age`→0 (PATTERN guard-cannot-fire filed); hub dead-branch kicks inside the relaunch throttle gap ×5 (thiquet restarts=1 is that, not a wedge); `check`/`reinstall` render with `which rmx`, no dev-tree refusal; stop_daemon drops the discovered pid; `_touch` swallows OSError; bug-014 fakes unregistered. Plan may NOT flip completed. |
| dfc0e62 (768f868..dfc0e62, remedy a035117, judged at 7bca4bb; plan-3 r5) | 2026-09-15 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 5 (1B/2S/2M) | [[bsd-plan3-verbs-parity-r5-dfc0e62]] — all seven r4 findings closed at the quoted lines and reproduced on BOTH simulations (recall-state busy on the silent socket, every `memory` action / MCP recall default / dense path / promote / `memory_partition` bounded 10–30 s on a held writer with the slot untouched, CLI `get` 10 s and `recall` 15 s to the replica, M1b/M2 fail as claimed); BULLSHIT #b-1: `rmx memory list` and `rmx memory search` never call the `memory` verb whose `MEMORY_READ_ACTIONS` names them — bare ping gate + 60 s × 3 → 190.2 s each on a held writer with the replica present (partial bound 6th); SKETCHY: #m-6 closed on `_SilentDaemon` only — held writer without a replica prints "reading the replica" then the reader error; `federated_query` 60 s and the held store dropped from `projects` AND `skipped` (Q14); MEH: recall twin double-probes (5 + 10 s of 30), registry two seams short (2nd gap in plan-3) |
| ffed3df (ef4365a..ffed3df, plan-4 r4) | 2026-09-15 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 6 (1B/3S/2M) | [[bsd-plan4-daemon-resilience-r4-ffed3df]] — all seven r3 findings closed, the two big ones in behaviour: 0 launchd SIGKILLs on any `com.refmatrix.*` label since the deploy (24 in the 4 h before) and this project's daemon drained 29 s and exited 0 where 5 s would have killed it; `shutdown.started` is written first in the finally and `_await_shutdown` reads a value the shutting-down daemon still produces, proven through `_check` on a real drain (guard-cannot-fire's three rules met); registry complete, dev-tree test makes 0 launchctl calls, 114 passed at HEAD. OPEN #b-1: the HUB plist is the one plist with no `rmx=`, no `_refuse_dev_tree` and no `check` — `rmx hub launchctl install --force` from a dev shell writes it from `which rmx` (probe: ProgramArguments[0] = the dev binary, ExitTimeOut 45 added by the same commit two lines below); `binary_identity` unknown → `dev_tree: None` → NO refusal, so a mistyped `--rmx` rewrites every drifted plist; `stop_daemon`'s 5 s default survives at 4 call sites (`daemon stop`, vacuum, upgrade, `_respawn` which also discards the False) — measured False at 5.0 s on a 9 s drain; tests append to the live `~/.refmatrix/hub.log` (43 lines) and the new marker-wait line names no root; `allow_dev` unreachable; session-indexer label still `exit timeout = 5`. Plan may NOT flip completed. |
| ffed3df (f170a4c..ffed3df + d576a81, e4ea1ad; plan-2 r9) | 2026-09-15 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 5 (1B/2S/2M) | [[bsd-plan2-hooks-reproducible-r9-ffed3df]] — the replica-first partition probe is real and reproduces live (SessionStart 0.22 s / 10 rows under a held writer, was `[]`; four of five of my mutations kill a test apiece) and r7's socket-timeout leak is closed on a real socket (`info(1 s)` leaves the client at 60 s); but the rerank leg is STILL dead on the deployed hook — 0 of 7 runs at 05:48–06:02 under load 3.5–5.8, every one spending the whole 5 s budget to return exactly what `--no-rerank` returns in 0.79 s, because the capped pool costs the shared worker 3.7–14.1 s and Q11's 1.7–2.8 s was measured 40 s after the fleet relaunch; the rerank also eats the global leg's budget and the hook blames a daemon it never asked; the replica-first probe was applied to WRITE routing too (`memory_add`) with no freshness bound |
| ffed3df (dfc0e62..ffed3df, remedy 26404fe; plan-3 r6) | 2026-09-15 | Todd Holley / Fable 5.1 / orchestrator | DIRTY | 6 (2B/2S/2M) | [[bsd-plan3-verbs-parity-r6-ffed3df]] — all five r5 findings closed and reproduced on BOTH simulations in all four replica states (`list`/`search` 10.0 s with ONE op and the replica's rows, `_read_store` never hands back the write proxy, `federated_query` 20 s + named skip, one partition probe per command, registry complete; my M1/M6/M7/M8 each fail the tests that claim them); OPEN #b-1: `rmx memory get --degree 1` raises a bare `NameError` on the DEPLOYED build in every daemon state — the r5 remedy dropped `daemon_mod`'s import thirty lines above the call, no test invokes the flag; #b-2: `rmx memory promote` is the group's third read path, still bare-ping + 60 s × 3 = 180.2 s and ends with an EMPTY message (the verb's own promote read is 90.2 s at retries=2 vs Q11's retries=0) — 7th partial-bound, 2nd twin-bypass in plan-3; SKETCHY: the 4th fan-out `federated_concept` drops a busy store so `rmx canon find` says "no live project hosts" (r3 #b-2 one command over); three of the four new "said, never mute" branches in `federated_where` survive deletion with the suite green (M3/M4/M5); MEH: `_RECALL_FORWARD` partition forward unguarded (M2: 59 passed), interactive recall now spends the whole budget on the stalled daemon (30.0 s vs r5's 15.0 s). Plan may NOT flip completed. |
| 9494f1b (76876c1..9494f1b) | 2026-09-16 | Todd Holley / Opus 5 (1M) / orchestrator | DIRTY | 15 (4B/7S/4M) | [[bsd-plan12-open-bugs-9494f1b]] — the self-healing half of bug-024 survives every probe (the adoption swap really is safe against an in-flight call; the deadline drop runs through the real worker loop; the derive stamp works on real DuckDB and reads STALE in the incident's own state; the pyc guard is wired) and the plan was NOT flipped to completed before the gate — but both NEW instruments sample their number at the wrong instant: `queue_depth` is read after the queue drained (reports the callers BEHIND you, prints 0 with one caller ahead — proven with two socketpair probes against the real `_handle`) and `render_phase_report` labels every interval with the phase that ended at its START (blames `store-bind` for `build_context`'s 2 s — proven on a run with known costs), with the measurement doc documenting the shift instead of fixing it; `private_workers` alerts nobody and no task schedules the hot gate; mock registry untouched for a 4th round. Plus lexicographic `min()` printing `derive: 0.71.0 (running 0.71.0) — stale`, `derive_status` missing from CLI_OPS, and the scan-prompt hook path changed under a "hook path unchanged" claim |
| c92274b (9494f1b..c92274b, plan-12 r2) | 2026-09-16 | Todd Holley / Opus 5 (1M) / orchestrator | DIRTY | 7 (1B/3S/3M) | [[bsd-plan12-r2-c92274b]] — a strong remedy: both r1 blockers fixed and PROVEN with the same probes that broke them (`queue_depth` reads 2 where it read 0; `build_context` billed for its own 2 s), 12 of 13 mutations caught, the two deletable renderers now have CliRunner tests (writing them caught rich eating `[memory-p]`), registry + deferral rows repaired, A/B harness committed and re-run by me against the real oracle (541/672/509/509 reproduce exactly), full suite 2011 passed with the contaminated run preserved. BULLSHIT: `ahead` moved to the right side of the `finally` but is still on the wrong side of `_worker`, which holds `_lock` across a 6.6 s cold model load — probe reads `queue_depth: 0` with two callers ahead, and the test stubs out the blocking section. SKETCHY: the hot gate is MY r1 recommendation and it turns all 8 stores permanently hot within ~2 days (34 version bumps / 10 days, 6 idle stores); the published 700-char split arm is 338, not 500, in the number that chose WINDOW_MIN_CHARS and is quoted as "measured, not chosen"; the `all: True` partition walk is the one mutation that survives and its composite re-renders the `0.71.0 (running 0.71.0) — stale` line #s-1 removed |
| 34aeddf (c92274b..34aeddf, plan-12 r3-r5) | 2026-09-16 | Todd Holley / Opus 5 (1M) / orchestrator | CLEAN | 0 | [[bsd-plan12-r3-r5-close-34aeddf]] — closing audit over three remediation rounds. Every r1/r2 finding closed and each fix reproduced by the probe that FOUND it, not by its author's test: cold-load `queue_depth` 0→2, healthy worker survives the drop, `memory-p` reaches the hub, mtime-preserving restore STALE HIT→miss, release day reads `derive_unstamped`, `worst_partition`=zzz-p@0.49.1. **6 of 6 reverts RED** — and V5's first attempt was a BROKEN mutation (no-op) that read as a surviving gap until re-run. Answered three author questions with measurement: the hash cache DID return a stale answer (bug-037's `(mtime,size)` identity rebuilt inside bug-039's detector, fixed with ctime+ino); the partition walk is 1.6 ms/partition at a fleet max of 3 = 0.27% duty cycle; the NULL-hash flood is unreachable — `derive_stamps` exists in 0 of 8 stores. Suite 2026 passed/0 failed at ca884b2. Four identifiers were wrong across the plan, two of them fabricated shas. Plan stays in-progress: deploy acceptance, 25 unpushed commits, bug-041 verify |
