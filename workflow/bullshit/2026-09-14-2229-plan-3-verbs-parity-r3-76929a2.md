---
gmd: "0.1"
id: bsd-plan3-verbs-parity-r3-76929a2
title: "ch-bsd findings — plan-3 verbs parity r3 (209270d..76929a2 + 0036d80): both r2 blockers closed at the quoted lines; the save-state verb still opens the store under a busy daemon and the `skipped` leg dies in the CLI"
tags: [bsd, findings, plan-3, verbs, mcp, cli, remedy-r3]
severity: BULLSHIT
plan: plan-3-verbs-parity
task: task-3.1-plan-3-verbs-parity, task-3.2-plan-3-verbs-parity, task-3.3-plan-3-verbs-parity
metadata:
  node_type: bsd-report
  commit: 76929a2
  remedy_commit: 80021a48ee5a9873b7b3e5dc2d0288bd13fc465d
  followup_commit: 0036d80f9373f4b0e0c5c4174b48572f885993eb
  range: 209270d..76929a2 (plan-3 content = 80021a4) + 0036d80
  deployed: 0.69.1 at ~/refmatrix (191e510), verified `rmx version -v` code=/Users/tholley/refmatrix/src
  verdict: DIRTY
  findings: 5
---

# ch-bsd findings — plan-3 remedy round 2 re-review (merge remedy-plan-3-r2) {#root}

**Commit:** 76929a2 (merge of 80021a4 `fix(verbs): typed busy/absent daemon states; one hub boundary that never swallows; wiring gate proves options and rendering (0.69.1)`) + 0036d80 (`test: ping fakes accept the classifier's keyword arguments`)
**Date:** 2026-09-14 22:10 PDT (audit 22:22–22:30)
**Author:** Todd Holley / Fable 5.1 / orchestrator
**Files changed (plan-3 content):** 13 (src: verbs.py +101, cli.py ±63, search.py +30, mcp.py ±7; tests: 1 new, 3 adjusted; 0036d80: 4 test fakes)

rel: amends -> [[bsd-plan3-verbs-parity-r2-209270d]]
rel: evidence-for -> [[plan-3-verbs-parity]]
rel: evidence-for -> [[impl-remedy-plan-3-verbs-parity]]

## Round-2 findings, verified against code, tests, scratch mutations and the live fleet {#r2-status}

Every check below was run by me at 76929a2 (plan-3 files are byte-identical at the current HEAD ebdd9c7 except the `hub_queues` twin, which plan-1 r5 simplified to the same `VerbError` shape); none is taken from the summary.

- **#b-1 CLOSED — one hub boundary, and it raises.** `verbs._hub_rpc` raises `VerbError` on `ok:false` (with the hub's `error`), on `TimeoutError`/`socket.timeout`/`OSError`, and on a non-dict answer; `queues` is a one-liner over it; all ten bus verbs go through it; `_bus_verb` and `hub_queues` render the `ClickException`. `tests/test_plan3_remedy_r2.py` runs `queues` against a real socket (`_QueuesHub`, registered) and records the `rmx bus pub` refusal as a message, not a `KeyError`. The remedy's mutation A (swallow again) fails both hub-boundary tests in `pytest-plan3-r2-mutations.log`. Live on the busy fleet at 22:25:28: `rmx hub queues` rendered a table in 3 s. {#v-b1}
- **#b-2 CLOSED at the quoted lines — and open one hop away, see [[#b-1]] and [[#b-2]].** `VerbBusyError`/`VerbAbsentError` exist; `require_daemon` classifies through `discovery.daemon_status`; `_call` uses it; the four string matches are gone (`grep '"daemon not running" in'` → 0 hits in src); `memory add`, `memory get`, `ingest-status`, `memory recall` and `mcp._t_memory_add` dispatch on the type; the bootstrap fallback fires on `VerbAbsentError` only (the test asserts no `catalog*.duckdb` appears on a `_SilentDaemon`, and mutation B fails it). `federated_where`/`federated_locate` return `skipped`. **Live, against the refmatrix daemon while it was genuinely busy (pid 30867 alive, not answering, 22:25):** `rmx ingest-status` → `daemon busy pid=30867 … retry shortly` (exit 1, 2 s); `rmx memory get …` → same (7 s); `rmx memory recall --recent -k 1 --json` → same (6 s). No "not running" anywhere. {#v-b2}
- **#s-3 CLOSED.** `test_cli_twin_calls_its_verb` now binds the recorder's arguments against `inspect.signature(v.fn)`, compares an expected-kwargs dict per option-carrying argv (23 twins, all with options where the verb has any), and requires a canary from the canned result in the CLI output. `rmx_focus` returns `dialogue`/`milestones`/`last_line`/`events_total` and `focus context` renders those (the `s.all_events()` loop is gone from the twin). The r2 mutations B/C (render from the ring; drop `--status`) would now fail on the canary / the `status` kwarg. {#v-s3}
- **#s-4 CLOSED.** `pytest-plan3-r2-full.log` is recorded at the merge sha (5 failed / 1542 passed: `graph_landing` known + the four ping fakes 0036d80 fixes, whose fix log `pytest-plan3-r2-fakes.log` shows 26 passed / 1 known); `pytest-plan3-r1-mutations-2.log` records the two r1 mutations that were "observed live" (bug-007 revert fails `test_locate_verb_finds_an_ingested_file_by_basename` after 61 s; the shape row fails `test_no_twin_exclusions_are_honest`). RED 7 failed / 1 passed, GREEN 177 passed, both logged. {#v-s4}
- **#m-5 CLOSED.** `assert "jobs" in verbs.ingest_status(live)`; `test_queues_positive_path_on_a_real_socket`. {#v-m5}
- **Registry:** `_QueuesHub` and `_PingOnlyDaemon` rows present. **Plan status honest:** `in-progress`, tasks 3.1–3.3 `pending`. **Deferral sweep** over the 13 changed files: no hard or soft deferral added (`git show 80021a4 -- src/ | grep '^+'` against both pattern families → 0). **Fleet hygiene:** plan-3's tests write no `rmxm-`/`rmxo-`/`rmxs-` line into `~/.refmatrix/hub.log`; the r2 `rmxw-*` churn was plan-4's and 23549ae moved it under a tmp `RMX_HOME`. {#v-misc}

## Findings {#findings}

### BULLSHIT (4th sighting of busy≠absent, on the write path): `rmx_save_state` opens the active slot under a busy daemon through `handoff._promote_digest` {#b-1}

**File:** `src/refmatrix/handoff.py:309-321` (`_promote_digest`: `if daemon_mod.ping(root): … else: Store(root) … add_memory`), reached from `src/refmatrix/verbs.py:737-740` (`save_state` → `handoff.compose_save_state(promote=promote)`), `handoff.py:371-372`
**What:** Decision Q11 says "only `VerbAbsentError` may open the store in-process". The verb registry's own `rmx_save_state` (default `promote=True`, the CLI `rmx save-state` default, the MCP tool default) reaches a bare-`ping` gate whose false branch opens `Store(root)` on the active catalog and writes the digest. A busy daemon fails the ping.
**Why it's bullshit:** Probe `workflow/review-output/pytest-bsd-plan3-r3-probes.log` on a `_SilentDaemon` root (real pid file + real listening socket that never answers): `handoff._promote_digest(root, s)` → `{'name': 'focus_summary_s1', 'id': 1}` and `catalog.duckdb` appears; `verbs.save_state(root, session="s2", memory_dir=…, lint=False, sync=False)` → `promoted={'name': 'focus_summary_s2', 'id': 1}` and `catalog.duckdb` appears. That is the second writer on a held catalog — the lock family in `workflow/bug_registry.md` and the reason #b-2 (r2) called the write path "the sharp end". The `memory add` fallback was fixed as quoted; the identical fallback that the plan's own `save_state` verb runs was not — [[bsd-impressions#imp-quoted-line-sibling]], third time in this plan. Exposure is real: `/save-state` runs at session end, right after the Stop-hook promote and a post-commit sync hold the writer (the r2 finding measured exactly that window). The PreCompact hook is not exposed (`--no-promote`); `focus summarize --promote` is not (plan-2 r2 removed its fallback). Escalation: busy≠absent is now r2-plan-2 (`--detach`), r3-plan-2 (`focus summarize`), r2-plan-3 (`_call`), r3-plan-3 (`_promote_digest`) — BULLSHIT with no grace.
**Evidence:** probe log above (`CATALOGS: [PosixPath('/tmp/rmxs-…/.refmatrix/catalog.duckdb')]` twice); `grep -n "daemon_mod.ping" src/refmatrix/handoff.py` → 309, 485; `grep -n "promote" src/refmatrix/cli.py` → `--promote/--no-promote default=True` at 10842.
**Fix:** `_promote_digest` classifies with `discovery.daemon_status` (or calls `verbs.require_daemon` — handoff already imports from verbs' neighbours): up → daemon `memory_add`; busy → `{"error": "daemon busy pid=… — digest not promoted", "name": name}` (the function already returns error dicts and `save_state` already renders `promoted.error`); absent → the in-process bootstrap. One test on `_SilentDaemon` asserting no `catalog*.duckdb` after `verbs.save_state(promote=True)` (the probe above is that test). Effort: minutes.
**Pattern match:** YES — [[bsd-impressions#imp-busy-absent-verb-layer]] (4th), [[bsd-impressions#imp-quoted-line-sibling]] (3rd in this plan).

rel: contradicts -> [[project-profile#constraints]]
rel: contradicts -> [[plan-3-verbs-parity#decisions-log]]

### BULLSHIT (3rd unread diagnostic field across plans): the `skipped` leg dies in `rmx locate`, and `federated_query` never grew one {#b-2}

**File:** `src/refmatrix/cli.py:7564-7573` (`locate`: `rows = res["results"]` … `if not rows: console.print("no matches")`; `skipped` read only under `--json`), `src/refmatrix/search.py:216-238` (`federated_query`: `if not daemon_mod.ping(root): continue` + `except Exception: pass`), `tests/test_verb_parity.py` (`CLI_INVOKE["rmx_locate"]` cans `"skipped": []`)
**What:** The r2 fix named three fan-outs (`federated_locate`/`federated_where`/`federated_query`) and "`rmx locate` prints it on stderr". Two fan-outs got the `skipped` leg; the CLI twin that operators run drops it; the third fan-out (the MCP-only `rmx_search` verb) still drops busy stores silently.
**Why it's bullshit:** Live at 22:25:11, refmatrix daemon busy: `rmx locate verbs.py` → `no matches`, exit 0, 2 s — the r2 scenario verbatim, one round later. Probe log: with the verb returning `skipped=[{reason: "daemon busy pid=1 …"}]` the twin prints `'no matches\n'` and nothing else; `verbs.search(root, "mentions:x")` on a `_SilentDaemon` root → `{'projects': []}`, no `skipped` key. The wiring gate cannot see this because the canned `rmx_locate` result carries an empty `skipped` — the same "the canary is in the part that renders" blind spot the gate was just upgraded to close. This is [[bsd-impressions#imp-unread-diagnostic-field]] for the third plan running (`identity_error`, `filed_subject_error`, now `skipped`): a remedy converts a silent drop into a dict key and the surface a human reads never prints the key. Escalated per the 3+ rule.
**Evidence:** `workflow/review-output/pytest-bsd-plan3-r3-probes.log` (`LOCATE OUTPUT: 'no matches\n'`, `SEARCH OUTPUT: {'projects': []}`); live twin probe output in this audit's transcript (`=== rmx locate verbs.py @ 22:25:11` → `no matches`, `[exit 0] 2s`); `sed -n 7564,7573p src/refmatrix/cli.py`.
**Fix:** `locate` prints each `skipped` row on stderr (`# rmx: skipped <project> — <reason>`) in both modes and says "no matches (N stores skipped)" when results are empty and `skipped` is not; `federated_query` (and `federated_concept`, same shape) use `_live_roots()` and return `skipped`; `CLI_INVOKE["rmx_locate"]` cans a non-empty `skipped` with a canary in its reason so the gate proves the render. One `_SilentDaemon` test per surface. Effort: under an hour.
**Pattern match:** YES — [[bsd-impressions#imp-unread-diagnostic-field]] (3rd), [[bsd-impressions#imp-quoted-line-sibling]].

rel: contradicts -> [[project-profile#constraints]]
rel: contradicts -> [[bsd-plan3-verbs-parity-r2-209270d#b-2]]

### SKETCHY: two bare pings survive in the file the remedy edited, and Q11 says they do not {#s-3}

**File:** `src/refmatrix/verbs.py:231` (`memory_partition`: `if daemon_mod.ping(root): … partition_list` else the non-legacy name), `verbs.py:372-375` (`attach_context`: `if not daemon_mod.ping(root): warnings.append("context: daemon not running for …")`)
**What:** Q11 records "`_call` and every verb that used a bare ping use it [`require_daemon`]". Two do not. `memory_partition` is "THE routing every memory verb must use" (its own docstring, naming the 0.21.1 / 0.25.x / 2026-09-06 split-brain family): on a busy daemon it logs a warning and returns `<project>` instead of `memory-<project>`. `_call` then classifies again; if that second classification succeeds (the daemon freed the lock between the two probes, a window of about two seconds) the write lands in the wrong partition. `attach_context` reports a busy daemon as "not running" in the `warnings` the CLI prints.
**Why it's sketchy:** Probe log: `memory_partition(silent.root)` → returns the project name, no `VerbBusyError`; `attach_context(silent.root, …, warnings=w)` → `['context: daemon not running for …; no bundles attached']`. I could not confirm whether any live store still carries a legacy `memory-<project>` partition (`rmx partition list` tracebacked on the busy daemon, see [[#observations]]), so the wrong-partition write is a code-path finding, not an observed one — hence SKETCHY. The decision text is wrong either way.
**Evidence:** `grep -n "daemon_mod.ping(" src/refmatrix/verbs.py` → 231, 372; `workflow/plans/plan-3-verbs-parity.md` Q11.
**Fix:** `memory_partition` calls `require_daemon` (busy raises; absent keeps the current default with the warning); `attach_context` classifies and says "busy" when it is. Correct Q11 to name the exceptions if any are kept.
**Pattern match:** YES — [[bsd-impressions#imp-fix-at-quoted-line]].

rel: contradicts -> [[plan-3-verbs-parity#decisions-log]]

### SKETCHY: "a socket timeout on an op is busy" is a claim without a test; `memory promote` bypasses it {#s-4}

**File:** `src/refmatrix/verbs.py:211-216` (`_call`: `except (TimeoutError, _socket.timeout, OSError) → VerbBusyError`), `verbs.py:1010` and `:1017` (`memory` action `promote`: bare `daemon_mod.call(memory_get, timeout=30.0)` and `hub_mod.global_call`), `tests/test_plan3_remedy_r2.py`
**What:** Q11's third clause. My scratch mutation D (`workflow/review-output/pytest-bsd-plan3-r3-mutations.log`: `_call` lets the raw socket timeout through) passes all 82 tests in `test_plan3_remedy_r2`, `test_verbs_migrated`, `test_plan2_remedy`, `test_verb_parity`. `_SilentDaemon` never answers the ping, so `require_daemon` raises before any op is sent; nothing exercises the op-timeout branch. `_PingOnlyDaemon` (answers ping, stalls every op) exists in `test_plan2_remedy.py` and is registered for exactly this state, and no plan-3 test uses it. Separately, the `memory` verb's `promote` action issues its `memory_get` and `global_call` outside `_call`, so a stalled writer there is a raw `TimeoutError` traceback at `rmx memory promote`.
**Evidence:** mutation log above (`82 passed`); `grep -ln "_PingOnlyDaemon" tests/*.py` → `tests/test_plan2_remedy.py` only; `grep -n "VerbBusyError" tests/*.py` → `test_plan3_remedy_r2.py` only.
**Fix:** One test: `_PingOnlyDaemon` root, `verbs.ingest_status(root)` (or any `_call` op with a short `timeout`) raises `VerbBusyError` naming the op; route `promote`'s two calls through `_call` / `_hub_rpc`-style wrappers.
**Pattern match:** YES — [[bsd-impressions#imp-existence-check-tests]] (a decision line with no test behind it).

rel: contradicts -> [[bsd-pattern-tests-prove-existence-not-wiring]]

### MEH: read twins refuse on busy although the lock-free reader is available, and pay three classifications to say so {#m-5}

**File:** `src/refmatrix/cli.py:9303-9308` (`memory_get`: `VerbAbsentError` → `_read_store()`, `VerbError` → refuse), `cli.py:9924-9925` (`memory_recall`: `VerbBusyError` → refuse), `src/refmatrix/verbs.py:1007` + `:211` + `:231` (`require_daemon` in `memory`, again in `_call`, plus the `memory_partition` ping)
**What:** `_read_store()` is the replica reader — "lock-free against the daemon's writer" by its own docstring — so the busy case is exactly where a read could proceed with a warning. Q11 decided type-dispatch, not reads-vs-writes. Measured live: `memory get` 7 s and `memory recall` 6 s to report busy, because the `memory` verb classifies, `_call` classifies again, and `memory_partition` pinged first (three probes at ~2 s each: 3 attempts × 0.5 s + backoff). The r1 double-ping (#meh) is back as a triple under busy. `rmx memory recall --session-start` and `--stdin-json` run in the foreground from the SessionStart and UserPromptSubmit hooks under the loudness contract; a busy daemon at session start is now a red hook error where it used to be a replica read.
**Fix:** Decide and record: on `VerbBusyError` the read twins fall through to `_read_store()` with `# rmx: daemon busy (pid …); reading the replica` on stderr, or keep the refusal and say why in Q11. Either way `memory` should not call `require_daemon` when it is about to call `_call`.

## Observations (not plan-3 findings) {#observations}

- **`rmx hub queues` renders an alive-but-busy daemon as `down`.** Live 22:25:28: `refmatrix | down | ?` while pid 30867 was alive and holding the lock. The producer is `hub._gather_queues` (`"daemon_up": st["up"]`, `hub.py:565`): it calls `daemon_status` and discards `busy`. Plan-1/4 territory — the plan-3 verb renders what the hub sends. {#obs-queues-busy-as-down}
- **`rmx partition list` is a raw traceback on a busy daemon** (`cli.py:3379`, bare `daemon_mod.call` at the 60 s × 3 default; it ran past my 100 s budget). Not a verb twin; [[bsd-impressions#imp-fleet-sum-timeouts]] territory. {#obs-partition-list}
- **Full suite at HEAD:** `workflow/review-output/pytest-bsd-plan3-r3-full.log` — **8 failed / 1548 passed in 257 s**, but the tree moved under the run: HEAD advanced from 191e510 to ebdd9c7 (plan-5 tasks 5.1/5.2 merged, plan-1 r5 merged) and the working tree has `task-5.3` checked out with `tests/test_memory_bridge.py` and `tests/test_save_state.py` modified. Six failures are `test_memory_bridge` (keys `skipped_non_gmd`/`waited`, "deprecated" alias — plan-5 RED/in-progress), one is `graph_landing` (known, plan 6.4), and `test_memory_recall_widen::test_explicit_since_is_honored_not_widened` fails only in the full run (a fresh `body of digest` row in its store) and passes in isolation (`pytest-bsd-plan3-r3-widen-isolated.log`: 9 passed / graph_landing) — order-dependent, not plan-3's. No plan-3 file fails. {#full-suite}
- **Audits keep the live daemon busy.** Every `tee workflow/review-output/*.log` lands inside the watched tree, so a 4-minute pytest run is 4 minutes of continuous sync on the refmatrix daemon — that is the busy window my live probes ran in. Useful for finding busy≠absent; bad for every hook in the user's session (memory `feedback_generated_data_off_watched_tree`). {#obs-logs-in-watched-tree}
- **Two dev-venv MCP servers** (pids 5555 / 10278) remain user-gated, unchanged. {#obs-mcp-servers}

## Verdict {#verdict}

**DIRTY — 5 findings (2 BULLSHIT, 2 SKETCHY, 1 MEH).**

The two r2 blockers are closed where they were quoted, and closed for real: the hub boundary raises with the hub's words (real-socket test, mutation fails, live table in 3 s), and the three CLI read/write twins report a genuinely busy daemon by type — I watched them do it live against pid 30867. The wiring gate is now a gate. What remains is the pattern this plan keeps producing: the fix lands at the quoted line and the sibling ships — this round the sibling is the plan's own `rmx_save_state` verb, which my probe drove to open `catalog.duckdb` under a busy daemon (the write the r2 finding called the sharp end), and the `skipped` leg the remedy built, which the `rmx locate` twin never prints (live: `no matches` on the same busy daemon, exit 0). **The plan may not flip to `completed`.** Both blockers are minutes of code plus one `_SilentDaemon` test each; the round-4 review should be short if the remedy also greps `handoff.py`, `search.py` and `verbs.py` for the remaining `daemon_mod.ping(` sites instead of the ones the finding quotes.

rel: contradicts -> [[project-profile#constraints]]
