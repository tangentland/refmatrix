---
gmd: "0.1"
id: bsd-plan3-verbs-parity-r4-768f868
title: "ch-bsd findings — plan-3 verbs parity r4 (3fa98bc..768f868): all five r3 findings closed at the quoted lines and each held under my own mutation; the plan's recall-state verb still calls a busy daemon stale, and the read fallthrough costs 390 s on a held writer"
tags: [bsd, findings, plan-3, verbs, mcp, cli, remedy-r4]
severity: BULLSHIT
plan: plan-3-verbs-parity
task: task-3.1-plan-3-verbs-parity, task-3.2-plan-3-verbs-parity, task-3.3-plan-3-verbs-parity
metadata:
  node_type: bsd-report
  commit: 768f868
  remedy_commit: 115a4aafd82d337d2aa393570542babfda469d2a
  range: 3fa98bc..768f868 (plan-3 content = 115a4aa)
  head_at_audit: 1aedb41 (moved from e1dba80 during the audit; plan-3 files byte-identical at both, committed and working tree)
  deployed: 0.69.1 at ~/refmatrix (e1dba80), verified `rmx version -v` code=/Users/tholley/refmatrix/src, fleet 8/8 v0.69.1
  verdict: DIRTY
  findings: 7
---

# ch-bsd findings — plan-3 remedy round 3 re-review (merge remedy-plan-3-r3) {#root}

**Commit:** 768f868 (merge of 115a4aa `fix(verbs): busy-safe promote and reads; every fan-out reports skipped stores; queue rows say busy`)
**Date:** 2026-09-14 22:47 PDT (audit 23:22–23:55)
**Author:** Todd Holley / Fable 5.1 / orchestrator
**Files changed:** 10 (src: cli.py ±32, handoff.py ±11, hub.py +5, search.py ±14, verbs.py ±43; tests: 1 new (147 lines), 1 adjusted; summary, plan Q13/Q14, registry ±1)

rel: amends -> [[bsd-plan3-verbs-parity-r3-76929a2]]
rel: evidence-for -> [[plan-3-verbs-parity]]
rel: evidence-for -> [[impl-remedy-plan-3-verbs-parity]]

## Round-3 findings, verified against code, tests, scratch mutations and probes {#r3-status}

Every check below was run by me at HEAD 1aedb41 (the plan-3 files `verbs.py`, `handoff.py`, `search.py`, `mcp.py` are unchanged since 768f868 — `git diff 768f868..HEAD --stat` on them is empty — and the working tree's in-flight edits to `cli.py`/`discovery.py`/`hooks.py` touch none of the plan-3 regions). Suites: `workflow/review-output/pytest-bsd-plan3-r4-suites.log` — **128 passed in 109 s** (`test_plan3_remedy_r3`, `_r2`, `_remedy`, `test_verb_parity`, `test_verbs_migrated`, `test_plan2_remedy`, `test_save_state`). Mutations on a scratch copy of the tree (never the dev tree, never a live store): `pytest-bsd-plan3-r4-mutations.log`.

- **#b-1 CLOSED.** `handoff._promote_digest` classifies through `verbs.require_daemon`; only `VerbAbsentError` opens the store; a `VerbBusyError` reaches the outer `except` and becomes `{"error": "VerbBusyError: daemon busy pid=…"}`. `test_promote_digest_reports_busy_and_never_opens_the_store` runs the real function AND `verbs.save_state(promote=True)` on a `_SilentDaemon` and globs for `catalog*.duckdb`. My mutation M1 (busy takes the absent branch) fails it. On a `_PingOnlyDaemon` (held writer) the probe `pytest-bsd-plan3-r4-pingonly.log` shows `catalogs: []` after the promote — no slot open in either busy state. {#v-b1}
- **#b-2 CLOSED.** `federated_concept` and `federated_query` take their roots from `_live_roots()` and return `skipped`; `verbs.search`/`verbs.where` carry it (probe: `search → {'projects': [], 'skipped': [{… 'daemon busy pid=85903 …'}]}`); `rmx locate` prints `# rmx: skipped <project> — <reason>` on stderr in both modes and says `no matches (1 store skipped — see stderr)`. `test_locate_cli_prints_skipped_stores` and `test_federated_query_and_concept_carry_skipped` run on the real silent socket; mutations M4 (query drops `skipped`) and M9 (`locate` prints only under `--json`) each fail their test. Live at 23:26 the fleet was idle, so the busy render was not observed live this round; the silent-socket test is the evidence. {#v-b2}
- **#s-3 CLOSED.** `memory_partition` raises `VerbBusyError` on busy (probe: 2.0 s) and returns the project default on absent; `attach_context` names the typed state in `warnings`. Mutations M2 (busy returns the default) and M8 (bare ping, "not running") fail their tests. `grep -n "daemon_mod.ping(" src/refmatrix/verbs.py` → 0 hits. Q13 records it. {#v-s3}
- **#s-4 CLOSED.** `test_socket_timeout_on_an_op_is_busy` runs `verbs.ingest_status` on a `_PingOnlyDaemon` and expects `VerbBusyError` naming the op; mutation M3 (`_call` lets the socket timeout through) fails it after 61 s. The `memory` verb's `promote` action goes through `_call` and its hub write is wrapped into a `VerbError`; mutation M10 (unwrap the hub write) fails `test_promote_action_goes_through_the_typed_wrappers`. The wrapper is not on the OTHER promote — see [[#s-3]]. {#v-s4}
- **#m-5 CLOSED as quoted — and see [[#b-2]].** `memory get` and interactive `memory recall --recent` catch `VerbBusyError` and read `_read_store()` with `# rmx: … ; reading the replica` on stderr; hook modes still degrade to empty (mutation M7 removes the hook guard and plan-2's `test_recall_hook_modes_are_bounded_and_degrade_to_empty` fails). Mutation M5 (busy refuses again) fails `test_memory_get_reads_the_replica_when_the_daemon_is_busy`. Q13 records the decision. {#v-m5}
- **Observation (queues) CLOSED.** `hub._gather_queues` sets `daemon_busy`/`identity: unknown` on a busy row, `rmx hub queues` renders `busy`; mutation M6 fails `test_gather_queues_marks_a_busy_daemon_busy`. {#v-obs}
- **Deferral sweep** over the 10 changed files: no hard or soft deferral added (`git show 115a4aa` `+` lines against both families → 0; the three `Phase N` hits in `cli.py` are the memory-list algorithm's step comments, not deferrals). **Plan status honest:** `in-progress`, tasks 3.1–3.3 `pending`. **Registry:** `_SilentDaemon`/`_PingOnlyDaemon` rows present; the remedy's own new patches are not — [[#s-5]]. {#v-misc}

## Findings {#findings}

### BULLSHIT (busy≠absent, 5th sighting in the plan-3 lineage, in the plan's own verb): `rmx_recall_state` reports a busy daemon as a stale pid, and the CLI's `busy` render branch has had no producer since 0.41.0 {#b-1}

**File:** `src/refmatrix/handoff.py:494` (`compose_recall_state`: `"running": bool(pid) and daemon_mod.ping(root)`), `:506-507` (`anomalies.append(f"daemon pid {pid} present but socket unreachable (stale)")`), `src/refmatrix/cli.py:11177-11180` (`recall-state` render: `busy pid` branch on `d.get("busy")`), `src/refmatrix/verbs.py` (`recall_state` → `handoff.compose_recall_state`)
**What:** The r3 verdict said, in as many words, that round 4 "should be short if the remedy also greps `handoff.py`, `search.py` and `verbs.py` for the remaining `daemon_mod.ping(` sites instead of the ones the finding quotes." `handoff.py` has two: line 309 was fixed; line 494 was not. It gates the daemon line of the resume report that `rmx recall-state`, the `/recall-state` command and the MCP `rmx_recall_state` tool (task 3.2's verb) all render.
**Why it's bullshit:** Probe `workflow/review-output/pytest-bsd-plan3-r4-probes.log` on a `_SilentDaemon` root: `compose_recall_state` → `daemon={'running': False, 'pid': 85903}`, `anomalies=['daemon pid 85903 present but socket unreachable (stale)']`. The CLI then prints `stale pid 85903` and the anomaly line — the exact wording plan-4 r1 traced a needless kill to ("cliquet read `stale pid (socket unreachable)` while a direct RPC answered in 0.1 s", `discovery.daemon_status`'s own docstring). The render has carried a `[yellow]busy pid {d['pid']}[/]` branch since a4f021f (0.41.0, "stop calling a busy daemon dead") that reads `d.get("busy")` — and no producer has ever set `busy` in `daemon_info` (`git show a4f021f -- src/refmatrix/handoff.py` is empty; `grep -n '"busy"' src/refmatrix/handoff.py` → 0). That is dead code by rule 1 and the unread-field pattern inverted: a reader with no writer. This lineage (r2 `_call`, r3 `_promote_digest`, now `compose_recall_state`) is [[bsd-pattern-busy-is-not-absent]] and the PATTERN's escalation applies: no grace.
**Evidence:** probe log above; `sed -n 490,508p src/refmatrix/handoff.py`; `sed -n 11176,11180p src/refmatrix/cli.py`; `tests/test_save_state.py:185` asserts `running is False` on a root with NO daemon only — no test puts a live pid behind the field.
**Fix:** `daemon_info = discovery.daemon_status(root)`-shaped: `{"running": st["up"], "busy": st["busy"], "pid": st["pid"]}`; the anomaly says `daemon pid N alive but not answering (busy)` when `busy` and keeps `(stale)` only when the pid is dead; the existing render branch then lights up. One `_SilentDaemon` test on `verbs.recall_state` asserting `daemon["busy"] is True` and no "stale" in `anomalies`. Effort: minutes.
**Pattern match:** YES — [[bsd-pattern-busy-is-not-absent]] (5th in lineage), [[bsd-impressions#imp-busy-absent-call-tree]], [[bsd-impressions#imp-quoted-line-sibling]] (4th in this plan).

rel: contradicts -> [[project-profile#constraints]]
rel: contradicts -> [[bsd-pattern-busy-is-not-absent]]
rel: contradicts -> [[bsd-plan3-verbs-parity-r3-76929a2#verdict]]

### BULLSHIT (partial bound, 5th sighting → PATTERN escalation): the read fallthrough exists, and on a held writer `rmx memory get` waits 390 s to reach it; an MCP `rmx_memory_recall` holds the turn 210 s {#b-2}

**File:** `src/refmatrix/verbs.py:1062` (`memory`: `part = partition or memory_partition(root)` — `partition_list` at 10 s × 3 retries), `:1087` (`_call(root, op, payload, timeout=120.0)` — library default `retries=2`), `:209-212` (`_call` docstring: "`daemon.call` retries on timeout, so a 5 s bound with the library default is a 15 s wait"), `:608-619` (`memory_recall` signature: `timeout: float | None = None` — the MCP default), `src/refmatrix/cli.py:9483-9491` (`memory get`: no budget, falls through only after the verb gives up)
**What:** The commit is titled "busy-safe … reads". Its test for the read fallthrough uses `_SilentDaemon` (fails the ping in ~2 s). The registered "daemon holding the writer lock" simulation, `_PingOnlyDaemon` — used in the same test file for #s-4 — was not run against a single read twin.
**Why it's bullshit:** Probe `workflow/review-output/pytest-bsd-plan3-r4-pingonly.log`: `verbs.memory(root, action="get", name="x")` on a `_PingOnlyDaemon` → `VerbBusyError … memory_get … did not answer within 120s` **after 390.4 s** (30 s of `partition_list` retries that end in a logged guess, then 3 × 120 s); `verbs.memory_recall(root, recent=True)` with the MCP default `timeout=None` → `VerbBusyError` **after 210.4 s** (30 s + 3 × 60 s). The lock-free `catalog.read.duckdb` that the fallthrough finally reads sat there the whole time. A held writer — a daemon in a long ingest or an index repair — is the busy state operators actually hit (every plan-4 and plan-5 incident this day was one); the silent socket is the restart window. `_call`'s own docstring names the multiplication; `memory_recall` already carries the deadline machinery (`_left`, `retries=0`) for the hook modes and the interactive/MCP callers get none of it. This is the shape of [[bsd-pattern-partial-bound]] (the bound stops at the surface the finding named: r3 measured the silent case at 6–7 s and the remedy fixed that case), 5th sighting across plans — the escalation rule makes it BULLSHIT on sight.
**Evidence:** pingonly log (`[390.4s]`, `[210.4s]`, the two `memory_partition: partition_list failed … (timed out); using 'rmxs-…'` warnings); `grep -n "_PingOnlyDaemon\|pingonly" tests/test_plan3_remedy_r3.py` → fixture + `test_socket_timeout_on_an_op_is_busy` only.
**Fix:** interactive read twins and the MCP recall get a default budget (`memory get`: `memory_partition(timeout=…)` + `_call(..., retries=0)` under a 10 s budget, then the replica with the stderr note; `rmx_memory_recall` default `timeout` ≠ None, e.g. 30 s, `_call(retries=0)` under it — the verb already does this when `timeout` is given). One `_PingOnlyDaemon` test per surface asserting the replica row arrives within the budget. Effort: under an hour.
**Pattern match:** YES — [[bsd-pattern-partial-bound]] (5th), [[bsd-impressions#imp-socket-sim-probe]] ("when a test patches … run the simulation" — here the wrong simulation was run).

rel: contradicts -> [[bsd-pattern-partial-bound]]
rel: contradicts -> [[plan-3-verbs-parity#decisions-log]]

### SKETCHY: Q13's "any transport exception is busy" is false for the plan's own save-state promote — `_promote_digest` calls the daemon bare, 90 s, and reports `TimeoutError` {#s-3}

**File:** `src/refmatrix/handoff.py:319` (`r = daemon_mod.call(root, "memory_add", args, timeout=30.0)` — library `retries=2`), `src/refmatrix/verbs.py:709` (`memory_recall` dense loop: bare `daemon_mod.call(root, "memory_get", …)` per hit)
**What:** r3 #s-4 said "promote bypasses the wrappers"; the remedy routed the `memory` verb's `promote` ACTION through `_call` and left `_promote_digest` — the promote that `rmx save-state` / `/save-state` / MCP `rmx_save_state` run — on a bare `daemon_mod.call`. The gate above it was fixed (#b-1), the call under it was not.
**Why it's sketchy:** pingonly log: `_promote_digest` on a held writer → `{'error': 'TimeoutError: timed out', 'name': 'focus_summary_s9'}` after **90.2 s**. No slot opened (good), but the error the user reads is the raw exception name, not "daemon busy … retry shortly", and the 90 s is three 30 s attempts against a lock that will not free in 30 s. `verbs.py:709` is the same shape one verb over (a per-hit timeout there is a raw `TimeoutError` out of the MCP tool, caught only by the dispatcher's generic `except`). SKETCHY not BULLSHIT because the write is refused correctly and save-state is interactive; it is the fourth quoted-line sibling in this plan.
**Evidence:** pingonly log; `grep -n "daemon_mod.call(" src/refmatrix/handoff.py src/refmatrix/verbs.py` → 319, 215 (`_call` itself), 245, 401, 709.
**Fix:** `_promote_digest` uses `_call(root, "memory_add", args, timeout=30.0, retries=0)` (busy typed, one attempt); the dense loop's `memory_get` likewise. One `_PingOnlyDaemon` test asserting `"busy" in promoted["error"]` and elapsed < 35 s. Correct Q13 to say which calls are wrapped.
**Pattern match:** YES — [[bsd-impressions#imp-quoted-line-sibling]].

rel: contradicts -> [[plan-3-verbs-parity#decisions-log]]

### SKETCHY: `memory_partition` raises on ping-level busy and GUESSES on op-level busy — the write that follows lands on the guessed partition {#s-4}

**File:** `src/refmatrix/verbs.py:243-253` (`partition_list` 10 s × 3 → `except Exception` → `logging.warning(… using %r)` → `return project`), callers `:566` (`memory_add` → `_call(memory_add, partition=memory_partition(root))`), `:865` (`change_subject`), `:1062`
**What:** The #s-3 fix made a failed PING raise. A daemon that answers the ping and stalls the op (the held writer) still takes the tolerant branch: 30 s, a log line, and the project default — exactly the branch the docstring says caused "the 0.21.1 / 0.25.x / 2026-09-06 split-brain family". `memory_add` then proceeds with that partition as soon as the lock frees.
**Why it's sketchy:** pingonly log shows the warning fire twice (`partition_list failed … (timed out); using 'rmxs-…'`). I checked all seven fleet stores with `rmx partition list`: none carries its own legacy `memory-<project>` partition today (refmatrix's store lists a `memory-viascope` repo partition, which is a different thing), so the wrong-partition write is a code-path finding, not an observed one. The decision text ("`memory_partition` raises busy") is true at one level and false at the other.
**Fix:** on the op-level timeout raise `VerbBusyError` (the caller "must not write around it", per the comment three lines up) instead of guessing; keep the guess only for an `ok:false` answer. One `_PingOnlyDaemon` test: `verbs.memory_partition(root)` raises busy.
**Pattern match:** YES — [[bsd-impressions#imp-quoted-line-sibling]] (fix at the ping, not at the op).

rel: contradicts -> [[plan-3-verbs-parity#decisions-log]]

### SKETCHY: the remedy's new patches are not in the mock registry {#s-5}

**File:** `tests/test_plan3_remedy_r3.py:88-107` (`test_promote_action_goes_through_the_typed_wrappers`: `monkeypatch` of `discovery.daemon_status` (dict), `discovery.store_name`, `daemon_mod.ping`, `daemon_mod.call` (lambda), `hub_mod.global_call` (raises)), `:150-152` (`discovery.discover_roots`, `verbs.queues` lambda), `workflow/test_mock_registry.md`
**What:** The registry diff in 768f868 is +1/−1 (row 39 gained "`tests/test_plan3_remedy_r3.py` (`verbs.queues` canned rows for the busy render)"). Row 41 (`discovery.daemon_status` dict) lists `test_plan2_remedy`, `test_plan1_remedy`, `test_plan3_remedy` — not the r3 file; `hub.global_call` has no row at all; the `daemon_mod.call` lambda in a plan-3 file is not under row 29 or 39.
**Why it's sketchy:** rule 3 — a `Mock`/`patch` without a registry entry. First registry omission in plan-3 (plan-1 went 3-for-3 and was escalated); this is the first strike here.
**Fix:** rows for `hub.global_call` (fake, raises) and `discovery.daemon_status` + `daemon.call` in the r3 file, with the graduation note that `_PingOnlyDaemon` covers the transport for real and these isolate the wrapping assertion.
**Pattern match:** YES — [[bsd-impressions#imp-registry-three-strikes]] (plan-1 lineage; first in plan-3).

rel: contradicts -> [[project-profile#constraints]]

### MEH: on busy with no replica file, a READ dies with the WRITE control point's message, after saying it is reading the replica {#m-6}

**File:** `src/refmatrix/cli.py:9483-9491` (`memory get`), `:10146-10156` (`memory recall --recent`: `rs = _reader_store() or _store()`), `:559-573` (`_read_store`: falls back to `_store()`, whose default is `write=True`)
**What:** Probe log: `rmx memory get x` on a busy root without `catalog.read.duckdb` → stderr `# rmx: daemon busy … ; reading the replica` then `Error: daemon busy … — writes go through the daemon; retry shortly`, exit 1 (5.9 s); `memory recall --recent --json --timeout 2` → the same pair. No slot is opened (plan-5's control point refuses), but the two lines contradict each other and the second names a write for a read. Not exposed on the live store (it has the snapshot file); exposed on a fresh store during its first daemon boot.
**Fix:** the twins check `_reader_store()` first and print "no replica yet; retry after the daemon's first snapshot" on `None`; or `_read_store()` raises a read-worded `ClickException` on busy instead of delegating to the writer.

### MEH: the wiring gate's canned `rmx_locate` result still carries `"skipped": []` {#m-7}

**File:** `tests/test_verb_parity.py:216-218`
**What:** r3 #b-2 asked for a non-empty canned `skipped` with a canary so the gate proves the render. The remedy proved the render with a dedicated real-socket test instead (my M9 fails it), which is the stronger evidence — but the gate itself still cannot fail on this key, and the next diagnostic key added to a verb result will have the same blind spot until the canned results carry every key populated.
**Fix:** `"skipped": [{"project": "p", "root": "/x", "reason": "CANARY-skip"}]` and `CANARY-skip` in the canary list.

## Observations (not plan-3 findings) {#observations}

- **The tree moved under the audit.** HEAD advanced from e1dba80 to 1aedb41 (plan-6 task 6.2, `660491d` "two fakes outrun by plan-3/plan-5 seams (bug-010)") and the working tree carries uncommitted edits to `discovery.daemon_status` (busy := `pid_is_rmx(pid)` with or without the socket, plan-5 r2 in flight), `cli.py` and `hooks.py`. None touches a plan-3 file or region; the suites and mutations above ran on a snapshot rsync'd at 23:31. Note for the plan-5 r2 audit: `pid_is_rmx` matches `"refmatrix" in ps -o command=`, so every process launched from `<repo>/.venv-eval/bin/python` — including pytest and the `_SilentDaemon` fixture's own pid — is "rmx" by path, not by role. {#obs-tree-moved}
- **bug-010** records two tests whose fakes patched `daemon_mod.ping` while `_live_roots` now classifies through `daemon_status` — the r2 remedy's seam change was correct and the suite carried the stale fakes for two rounds; the full-suite log was not re-read at HEAD in r3 (my own miss). {#obs-bug-010}
- **Live at 23:26 the fleet was idle** (8/8 daemons v0.69.1 up, `hub queues` 2 s, `locate` 1 s, `memory recall --recent` and `memory get` < 1 s), so no busy render was observed live this round; the previous audits' busy windows came from test logs landing in the watched tree, and this round's suites ran before the live checks. {#obs-idle-fleet}
- **Mutation harness note (my own).** In the M7 re-run and two later scratch runs `test_promote_action_goes_through_the_typed_wrappers` failed with the raw `OSError: hub down` — the M10 mutation's behaviour — although the scratch `verbs.py` was byte-identical to the dev tree. Cause: M10 swapped `except Exception` for `except VerbError` (same length), and the `cp`/`mv` restore landed in the same whole second as the mutation, so the `__pycache__` pyc compiled from the mutated source stayed valid (Python's pyc check is mtime-seconds + size). Touching the file fixed it (`pytest-bsd-plan3-r4-scratch-promote.log`: fails, then `1 passed` after a `cp` that bumps the mtime); the same selection on the dev tree passes 43/43 (`pytest-bsd-plan3-r4-order-dev.log`). M10's recorded failure was the live mutation, so the result stands; M7's hook-test failure is from the `cli.py` mutation (size changed, fresh pyc) and stands. Future scratch mutations run with `PYTHONDONTWRITEBYTECODE=1` or clear `__pycache__` between runs. {#obs-m7}

## Verdict {#verdict}

**DIRTY — 7 findings (2 BULLSHIT, 3 SKETCHY, 2 MEH).**

Round 3 is closed where it was quoted, and closed for real: every one of the five findings has a real-socket test behind it, and ten mutations on a scratch copy each failed exactly the test that guards them (M1–M6, M8–M10 on the r3 file; M7 on plan-2's hook test). No slot opens under either busy state on the write path — I drove the promote against both the silent socket and the held writer and the glob stayed empty. What remains is, again, the sibling: the r3 verdict named `handoff.py` for a ping grep and its second ping (`compose_recall_state`) still turns a busy daemon into `stale pid` with a dead `busy` render waiting under it; and the read fallthrough the remedy built is reachable only after 390 s on the busy state operators actually hit, because the fallthrough was tested on the silent socket and never on the registered held-writer simulation sitting in the same file. **The plan may not flip to `completed`.** Both blockers are small — a `daemon_status` call and a `busy` key in one dict; a default budget on two read surfaces — and round 5 should be the last if the remedy runs every read and write verb against `_PingOnlyDaemon`, not only `_SilentDaemon`, and greps `handoff.py` for `ping(` before writing the summary.

rel: contradicts -> [[project-profile#constraints]]
