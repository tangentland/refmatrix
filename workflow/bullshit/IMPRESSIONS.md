---
gmd: "0.1"
id: bsd-impressions
title: "ch-bsd impressions — refmatrix"
tags: [bsd, impressions]
---
# ch-bsd impressions {#root}
- 2026-09-14 (consolidated, runs 1–4) — Silent drops on memory paths (`|| true`, `parse_gmd→None→continue`, `except: pass`); two-ways-to-do-X (two bridges, two hook sources, two runtimes that were one); DuckDB index drift reachable from a READ via grep-learn; tests that monkeypatch the SUT; helpers tested in isolation cited as wiring proof; guards built for the fixed state not the incident; summaries claiming bookkeeping that did not happen; dead flags (`--enforce`); substring signatures widening the delete set; status flipped `completed` before the gate (twice, then corrected). Each of these has a dedicated impression memory under `memory/impressions/`. {#imp-consolidated-1}
- 2026-09-14 (consolidated, plan-3 r1 / plan-2 r2 / plan-1 r3 / plan-2 r3) — Tests that prove existence not the claim (`_click_cmd`, no_twin excluding every compared param); verb migrations that move code without tests and logs that disagree with the commit message; status-flip streak broke at two; hook costs claimed idle (0.13 s) vs live (55 s); remedies that follow my one-line fix verbatim and are still wrong; fixes at the quoted line while the sibling keeps the bug; registry omissions 2-for-2 in plan 1; diagnostic fields written and read by nobody; a bound on one RPC is not a bound on the command (`imp-partial-bound`, now a PATTERN at 4 occurrences). Dedicated memories: `impression_bsd_existence_check_tests`, `impression_bsd_cost_measured_idle`, `impression_bsd_partial_bound_guard`, `impression_bsd_tests_bypass_wiring`. Former anchors: imp-existence-check-tests, imp-migration-no-tests, imp-status-flip-corrected, imp-cost-measured-idle, imp-verbatim-fix, imp-plan2-r2-progress, imp-plan1-r3-closed, imp-registry-2-strikes, imp-unread-diagnostic-field, imp-partial-bound. {#imp-consolidated-2}
- 2026-09-14 (plan-2 r3) — Fixes land at the quoted line and not in the sibling: busy≠absent was fixed in `ingest-gmd --detach` (r2) while `focus summarize` ten lines from the new code kept the bare `ping` gate. Grep the diff's file for every other `if daemon_mod.ping(root)` before crediting a busy/absent fix. {#imp-fix-at-quoted-line}
- 2026-09-14 (plan-2 r3) — The pid-file + listening-socket simulation is now the reference probe for daemon-state paths; it found #b-1, #s-2 and #s-3 where the patched tests measured 1.25 s of a 7.4 s path. When a test patches `daemon_status` or `ping`, run the simulation instead of reading the assertion. {#imp-socket-sim-probe}
- 2026-09-14 (plan-1 r4) — The unread-field finding came back one hop upstream: r3 said `identity_error` had no readers; the remedy wrote three readers and four tests, and the PRODUCER (`_op_ping`) still never sends it. For any "field X is now consumed" remedy, trace X from where it is SET to where it crosses a process boundary, and demand a test that starts at the producer — a hand-written ping result proves nothing about the wire. {#imp-faked-wire-field}
- 2026-09-14 (plan-1 r4) — tests-bypass-wiring is now 3-for-3 in plan 1 (`_annotate_identity` r1, alert loop r2, `identity_error` r4) and the plan was flipped `completed` on the third. Escalation applied: the MEH became BULLSHIT. Any further plan-1 remedy whose tests patch `daemon_mod.call` gets the ping probe (`_op_ping` called for real with `runtime_identity` patched) before I read the assertions. {#imp-plan1-three-strikes}
- 2026-09-14 (plan-1 r4) — A sibling audit's 30 s `rmx grep` was enough to make `hub queues` traceback live. Fleet-wide status ops that sum per-daemon timeouts (8 × 12 s behind a 30 s rpc) are the next place the busy≠absent shape will show; check every CLI twin's except clause for the raw `TimeoutError`. {#imp-fleet-sum-timeouts}
- 2026-09-14 (plan-2 r4) — `imp-partial-bound` hit again one hook over: the detach probe is bounded, the `partition_list` (10 s × 3, takes `_store_lock`) and `ingest_gmd_start` (60 s × 3) calls under it are not — 210 s and a blank traceback. Third sighting is BULLSHIT. For any hook command, list its RPCs with `timeout`/`retries` per daemon state BEFORE reading the remedy's "one probe" claim; the simulation now needs a `ping`-answers-ops-stall mode as standard, not just silent. {#imp-partial-bound-2}
- 2026-09-14 (plan-2 r4) — `imp-unread-diagnostic-field` is now 2-for-2 across plans: `filed_subject_error` replaced `except: pass` and the verb layer drops the key. When a remedy converts a swallow into a dict key, grep the ONE-implementation verb that rebuilds the result — that is where keys die. {#imp-unread-field-2}
- 2026-09-14 (plan-2 r4) — Round-4 quality: 7/8 closed and every closure reproduced by the socket simulation; tests are the simulation, not patches; status honest. Residual is the next hook over, not the quoted one. {#imp-plan2-r4-progress}
- 2026-09-14 (plan-3 r2) — The remedy fixed the swallow the finding QUOTED (`global_recall_rows`) and shipped the identical `.get("result", {})` shape in `_hub_rpc`/`queues` twenty lines away, with `rmx bus pub` losing its `ok:false` guard on the way. Second sighting of fix-at-the-quoted-line; now: grep the diff's file for every `.get("result"`, `except Exception`, `ping(` sibling of the fixed line and probe each with a real `ok:false` before crediting a swallow fix. {#imp-quoted-line-sibling}
- 2026-09-14 (plan-3 r2) — busy ≠ absent reached the verb layer (3rd sighting → BULLSHIT): `_call` and `federated_locate` ping bare, and four CLI twins string-match "daemon not running" to pick a direct-store fallback (`memory add` WRITES through it). Three commands misreported in one two-minute restart window during the audit. Any diff that adds `"daemon not running" in str(e)` or a bare `ping(` gate is BULLSHIT on sight now; the probe is `discovery.daemon_status` during a hub kickstart. {#imp-busy-absent-verb-layer}
- 2026-09-14 (plan-3 r2) — Round-2 quality: 11/11 closed and each reproduced (recorder gate, 58 pairs, spawned daemon + real bus under a tmp RMX_HOME, bug-007 proven by my own mutation). The residue is one process boundary and one liveness check. The recorder gate proves CALL not RENDER: `sig.bind` on the recorder + expected kwargs per argv is the next rung. {#imp-plan3-r2-progress}
- 2026-09-14 (plan-1 r5) — Round 5 closed the wire: the producer-side test starts at `runtime_identity` raising and my own mutation of `_op_ping` fails it — the "test must begin at the producer" demand from r4 was met verbatim and it held. Keep demanding it; it is the one shape of test in this repo that has never lied. {#imp-plan1-r5-wire-closed}
- 2026-09-14 (plan-1 r5) — Registry omission went 3-for-3 in plan 1 (r1, r3, r5) and the escalation fired as armed. The remedies now fix code first and touch the registries never; a remedy commit that adds tests and does not `git show --stat` a registry is an omission on sight. Next: check the registry stat line before reading the tests. {#imp-registry-three-strikes}
- 2026-09-14 (plan-1 r5) — Deferrals are migrating from code comments into `workflow/deferral_registry.md` rows that name a plan and a task the task spec does not cover ("plan 6 task 6.4 (or a new 6.5)"). Rule 9a applies to registry rows exactly as to comments: open the named task spec and grep it for the deferred item before crediting the anchor. {#imp-registry-row-deferral}
- 2026-09-14 (plan-2 r5) — `imp-partial-bound` hit 3 and the PATTERN fired: the closure commit bounded the three hooks r2-r4 named and the hook that fires on EVERY prompt (`memory recall --stdin-json`) never had a bound — p50 20 s live, and r2 had the 24 s row in its own evidence table and filed nothing. The audit method itself was the partial bound: I timed the commands the findings quoted. On any hook plan, enumerate every generated memory-path command from the generator and time each against `_PingOnlyDaemon` before judging closure. {#imp-partial-bound-3}
- 2026-09-14 (plan-2 r5) — Round-5 quality: 5/5 closed, each reproduced on a real socket; the Stop bound has now been seen holding live (22:11:25, 5037 ms, exit 1). The remaining gap is not in the remedied code — it is the surface nobody listed. Cost regressions hide in cli.log per-day p50s: 11 ms in July, 20 s today, and no row in any report until now. Read the per-day p50 of every `source=hook` argv at the start of every hook audit. {#imp-plan2-r5-progress}
- 2026-09-14 (plan-3 r3) — busy≠absent 4th sighting, and it was in the CALL TREE not the diff: `verbs.save_state` → `handoff._promote_digest` still bare-pings and opens `Store(root)`. The remedy fixed every site the finding quoted and none it reaches. For any verb-layer remedy, walk each verb's call tree for `daemon_mod.ping(` / `Store(` before crediting "only absent opens the store"; the probe is `verbs.<verb>(silent.root)` + `glob("catalog*.duckdb")`. {#imp-busy-absent-call-tree}
- 2026-09-14 (plan-3 r3) — `imp-unread-diagnostic-field` is 3-for-3 across plans (`identity_error`, `filed_subject_error`, `skipped`) and now BULLSHIT on sight. A remedy that adds a key MUST ship the print, and the wiring gate's canned result must carry a non-empty value for the key so the canary proves the render — a canned `"skipped": []` is a gate that cannot fail. {#imp-unread-field-3}
- 2026-09-14 (plan-3 r3) — Round-3 quality: 5/5 closed as quoted, each reproduced live under a genuinely busy daemon (which the audit's own test logs under `workflow/review-output/` keep busy). Decision text (Q11) over-claims: "every verb that used a bare ping" left two; grep the decision's universal quantifiers against the file before reading the tests. {#imp-plan3-r3-progress}
- 2026-09-14 (plan-4 r1) — "Graceful, then kill after N s" was measured from the wrong end: `stop_daemon`'s stop-op branch needs a ping the watchdog's caller has already seen fail, so the 30 s grace is an idle wait and SIGTERM→SIGKILL is the hard-coded 2 s. For any courtesy-then-force path, write the caller's precondition beside the callee's branch conditions, then time first-signal→kill on a spawned daemon whose handler sleeps. {#imp-grace-before-signal}
- 2026-09-14 (plan-4 r1) — busy≠absent 4th sighting, inside the plan whose thesis is "never SIGKILL a working daemon": `_adopt_unsupervised` pings bare and a SIGSTOPped predecessor died in 3.54 s from `_reap_predecessor`; the same diff shipped `heartbeat_age` and did not use it there. PATTERN filed ([[bsd-pattern-busy-is-not-absent]]). Every ping gate in a diff now gets the SIGSTOP probe before I read the assertions. {#imp-busy-absent-4}
- 2026-09-14 (plan-4 r1) — An `ok:false` dict RETURNED from an op is `ok:true` on the wire (`_handle` wraps unconditionally); the abort path was green end to end and every test stopped at the store method. Also: a liveness file ticked by a thread that touches nothing the server does is interpreter-liveness (probe: no socket bound → "busy" ×8); and a "fleet relaunched" claim was 3/8 true (ping `version` per root, every time, before crediting any deploy claim). {#imp-ok-false-inside-ok-true}
- 2026-09-14 (plan-5) — busy≠absent reached the BRIDGE and took the store down: `_ingest_gmd_sync` → `_store()` gate on bare `ping`, so an alive-but-silent daemon sends the memory bridge into a direct `Store()` on the live slot. Four live runs in five minutes wrote catalog.B in-process (no `ingest-progress` in rmxd.log = no daemon job), then `Corrupted ART index — same row id inserted twice`. New standing probe for any write path in a diff: count `ingest-progress`/op lines in rmxd.log for the run; zero lines + exit 0 = in-process write. {#imp-bridge-direct-writer}
- 2026-09-14 (plan-5) — "One rule" rewrites drop the exceptions the old code carried: lenient parse replaced a walker that skipped `MEMORY.md` by name, and the flat index became a curated memory in the live store. When a diff deletes a branch of special-cases, list each deleted `if` and ask where it went; the covering test here seeded the very file and asserted nothing about it. {#imp-one-rule-drops-exceptions}
- 2026-09-14 (plan-5) — Counters that cannot move: `skipped_non_gmd` is 0 by construction on the bridge path (lenient ⇒ never None) and the test asserts `: 0`; a mutation deleting the counter passes 37 tests. For every new counter, find the input that makes it nonzero on the path the test exercises before crediting the assertion. Also: the coverage check for "another job did our work" matched on targets only — a non-memory ingest over the memory dir was accepted as the bridge's report with zero rows. {#imp-counter-cannot-move}
- 2026-09-14 (plan-5 r2) — Fifth busy≠absent: the classifier is now typed and honest for the SILENT-SOCKET state, but "absent" still means "no socket file", and `serve_forever` binds the socket LAST (after Store open + queued repair + snapshot + mirror). Every relaunch/respawn opens a multi-second window where the writer lock is held and `require_daemon` says absent. When a remedy says "only absent opens the slot", probe (live pid, no socket) before crediting it — the `Could not set lock` boots in daemon.stderr.log are what that window looks like. {#imp-boot-window-is-busy}
- 2026-09-14 (plan-5 r2) — Deleting a CLI command is a fleet change: `memory sync-disk` had a caller in `.claude/p20-0/compile_guardrails.py` (SessionStart hook, `>/dev/null 2>&1 || true`) in NINE projects, and the failure is invisible by construction. On every "X removed" remedy, grep `.claude/`, the cat-herder tree, and `~/github/*/.claude/` for the string — not just `src/` and `docs/`. {#imp-deleted-command-fleet-callers}
- 2026-09-14 (plan-5 r2) — GOOD: both r1 blockers landed for real and were proven live (daemon job logged, row forgotten, mutations fail). The busy-refusal shape (`require_daemon` → typed error → named message) is now the right primitive; the remaining gap is in the classifier under it, one line. {#imp-plan5-r2-progress}
- 2026-09-14 (plan-4 r2) — The "serving-derived" heartbeat is the ping wearing a file: the no-op waits `interval` on the cli pool and a late completion touches nothing, so four in-flight cli ops (the r1 live "busy" window) stop the beat while the daemon serves bg ops, and every r1 remedy that reads `heartbeat_age` (adoption `fresh`, reap yield, SIGKILL withhold) flips against the working daemon at 60 s. For any liveness tick: name the resource it waits on, saturate that resource with N=workers real ops, read the age for 60 s while serving something else. Touch-on-completion is the shape; touch-if-in-time is the ping. {#imp-liveness-tick-on-busy-queue}
- 2026-09-14 (plan-4 r2) — A gate "set by the plist" reached 6 of 8 plists: `relaunch-fleet` restarts processes, `launchctl.install` returns early unless `--force`, no plist `--check` exists, and the two labels left behind were this project's and the global store's — where a supervised start now exits 1 in 0 s (the orderly loop, reintroduced by the fix for a MEH). Before reading a render-contains-key test, list every live artifact with mtime and grep each for the key; then ask which deploy step would ever rewrite it. {#imp-gate-in-unrerendered-artifact}
- 2026-09-14 (plan-4 r2) — Round-2 quality: 12/12 r1 findings closed as quoted, each reproduced (real wire, real SIGSTOPped daemon, live fleet 8/8, live adoption on thiquet). The residue is the remedy's own new surface: the sibling forty lines down (`stop_daemon` grace-before-signal, 4th quoted-line sighting → BULLSHIT as armed), one new bare `ping(` gate, and two numbers (30 s grace vs 3×10 s drain) never put side by side. {#imp-plan4-r2-progress}
- 2026-09-14 (plan-2 r6) — `imp-partial-bound` hit 4: the bound was put where the DIAGNOSIS pointed (daemon RPCs, `retries=0`, one deadline) and the seconds were somewhere else — the rerank on the hub's shared worker inside the replica path (300 s socket, `info()` unbounded), the dense path's global leg, and a probe that runs before the clock starts. `--no-rerank` took the hook from 12.8 s to 0.96 s in one run. Before crediting any bound, decompose the command's wall by leg (env flags, `--no-X`, per-leg timers on the deployed venv) and time the exact deployed argv on the live store; a leg that is not a daemon RPC is still a leg. Live rows AFTER the fix (26.8 s, 18.9 s) beat the rows before it. {#imp-partial-bound-4}
- 2026-09-14 (plan-2 r6) — Test tolerances are being set just above the failure they exist to exclude: `elapsed < 2.5` for a 1 s budget passed at 2.01 s (additive probe + budget); the deadline test's `seen` population was `scope="project"` so the global leg's `retries=2` was never in it. Read the tolerance against the shape the finding named (2× budget, 3× budget) before crediting a timing assertion; a bound set below the operation's idle cost is a kill switch (`[]` every prompt), not a budget. {#imp-tolerance-admits-shape}
- 2026-09-14 (plan-3 r4) — Round-4 quality: 5/5 closed and each guarded by a test my own mutation fails (ten mutations, ten intended failures); the promote never opens the slot under either busy state. The residue is exactly what r3's verdict named and the remedy did not grep: `handoff.py:494`. When a verdict says "grep file X for Y", the next round's first act is that grep, before reading the summary. {#imp-plan3-r4-progress}
- 2026-09-14 (plan-3 r4) — Reader without a producer: `recall-state` has rendered `busy pid N` since 0.41.0 from a key nothing sets. The unread-field pattern has an inverse — grep every `d.get("<key>")` in a render for a producer, the same way I grep every producer for a reader. Also: run the REGISTERED simulation the state calls for — the read fallthrough was tested on `_SilentDaemon` (2 s to busy) and never on `_PingOnlyDaemon` (390 s to busy) sitting in the same file; the wrong simulation is a passing test that measures nothing. {#imp-reader-without-producer}
- 2026-09-14 (plan-3 r4) — Scratch-mutation hazard: a same-length edit restored within the same second keeps the stale pyc valid (mtime-seconds + size), so a mutation can outlive its restore and poison the next run. Always `PYTHONDONTWRITEBYTECODE=1` / clear `__pycache__` between mutations; a mutation result that persists after the restore is the harness, not the code. {#imp-stale-pyc-mutation}
