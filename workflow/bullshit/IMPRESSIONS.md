---
gmd: "0.1"
id: bsd-impressions
title: "ch-bsd impressions — refmatrix"
tags: [bsd, impressions]
---
# ch-bsd impressions {#root}
- 2026-09-14 (consolidated, runs 1-8) — Silent drops on memory paths; two-ways-to-do-X; DuckDB index drift reachable from a READ; tests that monkeypatch the SUT; helpers tested in isolation cited as wiring proof; guards built for the fixed state not the incident; dead flags; substring signatures widening a delete set; status flipped `completed` before the gate; existence-check parity gates; verb migrations without tests; hook costs claimed idle (0.13 s) vs live (55 s); remedies that follow my one-line fix verbatim and are still wrong; fix-at-the-quoted-line while the sibling keeps the bug; registry omissions; diagnostic fields written and read by nobody; a bound on one RPC is not a bound on the command; grace-before-signal; `ok:false` inside `ok:true`. Dedicated memories exist for each under `memory/impressions/`. Former anchors: imp-consolidated-1, imp-consolidated-2, imp-consolidated-3, imp-grace-before-signal, imp-busy-absent-4, imp-ok-false-inside-ok-true, imp-fix-at-quoted-line, imp-socket-sim-probe. {#imp-consolidated-0914}
- 2026-09-14 (plan-4 r1) — busy≠absent 4th sighting, inside the plan whose thesis is "never SIGKILL a working daemon": `_adopt_unsupervised` pings bare and a SIGSTOPped predecessor died in 3.54 s from `_reap_predecessor`; the same diff shipped `heartbeat_age` and did not use it there. PATTERN filed ([[bsd-pattern-busy-is-not-absent]]). Every ping gate in a diff now gets the SIGSTOP probe before I read the assertions. {#imp-busy-absent-4}
- 2026-09-14 (plan-4 r1) — An `ok:false` dict RETURNED from an op is `ok:true` on the wire (`_handle` wraps unconditionally); the abort path was green end to end and every test stopped at the store method. Also: a liveness file ticked by a thread that touches nothing the server does is interpreter-liveness (probe: no socket bound → "busy" ×8); and a "fleet relaunched" claim was 3/8 true (ping `version` per root, every time, before crediting any deploy claim). {#imp-ok-false-inside-ok-true}
- 2026-09-14 (plan-5) — busy≠absent reached the BRIDGE and took the store down: `_ingest_gmd_sync` → `_store()` gate on bare `ping`, so an alive-but-silent daemon sends the memory bridge into a direct `Store()` on the live slot. Four live runs in five minutes wrote catalog.B in-process (no `ingest-progress` in rmxd.log = no daemon job), then `Corrupted ART index — same row id inserted twice`. New standing probe for any write path in a diff: count `ingest-progress`/op lines in rmxd.log for the run; zero lines + exit 0 = in-process write. {#imp-bridge-direct-writer}
- 2026-09-14 (plan-5) — "One rule" rewrites drop the exceptions the old code carried: lenient parse replaced a walker that skipped `MEMORY.md` by name, and the flat index became a curated memory in the live store. When a diff deletes a branch of special-cases, list each deleted `if` and ask where it went; the covering test here seeded the very file and asserted nothing about it. {#imp-one-rule-drops-exceptions}
- 2026-09-14 (plan-5) — Counters that cannot move: `skipped_non_gmd` is 0 by construction on the bridge path (lenient ⇒ never None) and the test asserts `: 0`; a mutation deleting the counter passes 37 tests. For every new counter, find the input that makes it nonzero on the path the test exercises before crediting the assertion. Also: the coverage check for "another job did our work" matched on targets only — a non-memory ingest over the memory dir was accepted as the bridge's report with zero rows. {#imp-counter-cannot-move}
- 2026-09-14 (plan-5 r2) — Fifth busy≠absent: the classifier is now typed and honest for the SILENT-SOCKET state, but "absent" still means "no socket file", and `serve_forever` binds the socket LAST (after Store open + queued repair + snapshot + mirror). Every relaunch/respawn opens a multi-second window where the writer lock is held and `require_daemon` says absent. When a remedy says "only absent opens the slot", probe (live pid, no socket) before crediting it — the `Could not set lock` boots in daemon.stderr.log are what that window looks like. {#imp-boot-window-is-busy}
- 2026-09-14 (plan-5 r2) — Deleting a CLI command is a fleet change: `memory sync-disk` had a caller in `.claude/p20-0/compile_guardrails.py` (SessionStart hook, `>/dev/null 2>&1 || true`) in NINE projects, and the failure is invisible by construction. On every "X removed" remedy, grep `.claude/`, the cat-herder tree, and `~/github/*/.claude/` for the string — not just `src/` and `docs/`. {#imp-deleted-command-fleet-callers}
- 2026-09-14 (plan-5 r2) — GOOD: both r1 blockers landed for real and were proven live (daemon job logged, row forgotten, mutations fail). The busy-refusal shape (`require_daemon` → typed error → named message) is now the right primitive; the remaining gap is in the classifier under it, one line. {#imp-plan5-r2-progress}
- 2026-09-14 (plan-4 r2) — The "serving-derived" heartbeat is the ping wearing a file: the no-op waits `interval` on the cli pool and a late completion touches nothing, so four in-flight cli ops (the r1 live "busy" window) stop the beat while the daemon serves bg ops, and every r1 remedy that reads `heartbeat_age` (adoption `fresh`, reap yield, SIGKILL withhold) flips against the working daemon at 60 s. For any liveness tick: name the resource it waits on, saturate that resource with N=workers real ops, read the age for 60 s while serving something else. Touch-on-completion is the shape; touch-if-in-time is the ping. {#imp-liveness-tick-on-busy-queue}
- 2026-09-14 (plan-4 r2) — A gate "set by the plist" reached 6 of 8 plists: `relaunch-fleet` restarts processes, `launchctl.install` returns early unless `--force`, no plist `--check` exists, and the two labels left behind were this project's and the global store's — where a supervised start now exits 1 in 0 s (the orderly loop, reintroduced by the fix for a MEH). Before reading a render-contains-key test, list every live artifact with mtime and grep each for the key; then ask which deploy step would ever rewrite it. {#imp-gate-in-unrerendered-artifact}
- 2026-09-14 (plan-4 r2) — Round-2 quality: 12/12 r1 findings closed as quoted, each reproduced (real wire, real SIGSTOPped daemon, live fleet 8/8, live adoption on thiquet). The residue is the remedy's own new surface: the sibling forty lines down (`stop_daemon` grace-before-signal, 4th quoted-line sighting → BULLSHIT as armed), one new bare `ping(` gate, and two numbers (30 s grace vs 3×10 s drain) never put side by side. {#imp-plan4-r2-progress}
- 2026-09-14 (plan-2 r6) — `imp-partial-bound` hit 4: the bound was put where the DIAGNOSIS pointed (daemon RPCs, `retries=0`, one deadline) and the seconds were somewhere else — the rerank on the hub's shared worker inside the replica path (300 s socket, `info()` unbounded), the dense path's global leg, and a probe that runs before the clock starts. `--no-rerank` took the hook from 12.8 s to 0.96 s in one run. Before crediting any bound, decompose the command's wall by leg (env flags, `--no-X`, per-leg timers on the deployed venv) and time the exact deployed argv on the live store; a leg that is not a daemon RPC is still a leg. Live rows AFTER the fix (26.8 s, 18.9 s) beat the rows before it. {#imp-partial-bound-4}
- 2026-09-14 (plan-2 r6) — Test tolerances are being set just above the failure they exist to exclude: `elapsed < 2.5` for a 1 s budget passed at 2.01 s (additive probe + budget); the deadline test's `seen` population was `scope="project"` so the global leg's `retries=2` was never in it. Read the tolerance against the shape the finding named (2× budget, 3× budget) before crediting a timing assertion; a bound set below the operation's idle cost is a kill switch (`[]` every prompt), not a budget. {#imp-tolerance-admits-shape}
- 2026-09-14 (plan-3 r4) — Round-4 quality: 5/5 closed and each guarded by a test my own mutation fails (ten mutations, ten intended failures); the promote never opens the slot under either busy state. The residue is exactly what r3's verdict named and the remedy did not grep: `handoff.py:494`. When a verdict says "grep file X for Y", the next round's first act is that grep, before reading the summary. {#imp-plan3-r4-progress}
- 2026-09-14 (plan-3 r4) — Reader without a producer: `recall-state` has rendered `busy pid N` since 0.41.0 from a key nothing sets. The unread-field pattern has an inverse — grep every `d.get("<key>")` in a render for a producer, the same way I grep every producer for a reader. Also: run the REGISTERED simulation the state calls for — the read fallthrough was tested on `_SilentDaemon` (2 s to busy) and never on `_PingOnlyDaemon` (390 s to busy) sitting in the same file; the wrong simulation is a passing test that measures nothing. {#imp-reader-without-producer}
- 2026-09-14 (plan-3 r4) — Scratch-mutation hazard: a same-length edit restored within the same second keeps the stale pyc valid (mtime-seconds + size), so a mutation can outlive its restore and poison the next run. Always `PYTHONDONTWRITEBYTECODE=1` / clear `__pycache__` between mutations; a mutation result that persists after the restore is the harness, not the code. {#imp-stale-pyc-mutation}
- 2026-09-15 (plan-5 r3) — busy≠absent closed at the classifier after five sightings: the remedy that held was the one probed against a REAL `serve_forever` boot (poll `daemon_status` at 10 ms from `Popen` to `up`), not only against the lookalike child the tests use. For any daemon-state remedy, run one real boot on a tmp store and record the transition table before crediting the fixture. The `ps`-based identity is a fleet-wide name test (`rmx`/`refmatrix` in the command line), not a per-root one — acceptable because clean exit unlinks the pid file, but `_reap_predecessor` still kills by pid number alone. {#imp-real-boot-probe}
- 2026-09-15 (plan-5 r3) — "Loud hook" claims are proven by running the installed command verbatim (`CLAUDE_PROJECT_DIR=… bash -c '<hook string>'`) and diffing the artifact it writes: the guardrail compile changed two rule texts on its first live run since the alias returned — the silent failure had been hiding real policy drift, not a no-op. Also: the test suite leaks detached daemons on tmp roots (22 alive, `-p global`, ppid 1); `pgrep -f "daemon start --no-watch"` at audit start is now a standing check. {#imp-run-the-hook-verbatim}
- 2026-09-15 (plan-2 r7) — A per-call socket timeout is a socket STATE, not a call argument: `info(timeout=1)` left the shared-worker socket at 1 s and every later `rerank` inherited it, so the bound held (the r6 acceptance number) while the leg it was built around never completed — 0/12 hook runs reranked on a warm worker that answers in 1.0 s, with warnings that blamed the fleet. When a remedy adds a probe/step-specific timeout, read the transport for where the value is stored and demand a real-socket test whose second frame is the slow one. {#imp-probe-timeout-leak}
- 2026-09-15 (plan-2 r7) — "Bound holds" ≠ "answer arrives": measure the hook WITH and WITHOUT the leg (`--no-rerank` 0.96 s vs 1.9 s "with") — a difference equal to the timeout is a leg that always times out. Any closure that quotes only wall-clock gets the with/without diff before credit. {#imp-bound-holds-answer-dead}
- 2026-09-15 (plan-2 r7) — Round-7 quality: the deadline is real end to end (probe, verb, replica sockets, global one-attempt, PreCompact, env hook mode) and every r6 item reproduced live; the residue is one socket-state line copied to a second call site eleven minutes later. Registry omission is now 3-for-3 in plan 2 as it was in plan 1. {#imp-plan2-r7-progress}
- 2026-09-15 (plan-3 r5) — A bound put in the VERB is a bound on the MCP tool only: `MEMORY_READ_ACTIONS = {get, list, search}` bounded three actions and `grep "_verbs.memory(" cli.py` found one caller — the `list`/`search` twins keep a bare ping gate + 60 s × 3 and took 190 s each on the held writer, the r4 number one command over. For any "the verb is bounded" remedy, list the twins from the click tree and grep each for the verb call before crediting the CLI; the parity PAIRING covers one action of a thirteen-action verb, so the wiring gate cannot see this. {#imp-verb-bound-twin-bypass}
- 2026-09-15 (plan-3 r5) — Round-5 quality: 7/7 closed as quoted and reproduced on both simulations (silent + held, slot untouched, my mutations fail the CLI test); the #m-6 fix was tested on `_SilentDaemon` in a file whose `held` fixture six other tests use, and probe C on the held writer showed the same two contradicting lines the finding described. The wrong-simulation impression from r4 fired on the very finding that carried it: when the finding names a state, the closing test runs on THAT state's fixture. {#imp-plan3-r5-progress}
- 2026-09-15 (plan-4 r3) — launchd is a supervisor too: the plan reconciled the hub's kill grace (45 s) with the daemon's drain (40 s) and the deploy step (`relaunch-fleet` → bare `kickstart -k`) runs under `exit timeout = 5` — five SIGKILLs mid-drain in one night, in launchd's own log. For any "never SIGKILL a working daemon" claim, enumerate EVERY kill path (hub, adoption, `stop_daemon`, `_verify_relaunch`'s `kill -9`, launchd ExitTimeOut) and put each budget beside the drain; then read `log show --predicate 'process == "launchd"'` for `Sending SIGKILL` after the next relaunch — that log is the ground truth rmxd.log cannot see (a SIGKILLed daemon writes no `daemon stopped`). {#imp-launchd-is-a-supervisor}
- 2026-09-15 (plan-4 r3) — Guard-cannot-fire is a PATTERN now (4 sightings, 3 of them the heartbeat): `_await_shutdown` waits while `heartbeat_age < 45` and its only caller restarts when `heartbeat_age > 60`, and the daemon's first act of shutdown stops the beat. The test patched `heartbeat_age` → 0.0. For every new guard: caller's precondition beside the callee's condition; name the producer of the value and check it still runs when the guard runs; a test that patches the guard's input is not a test of the guard. {#imp-guard-cannot-fire-4}
- 2026-09-15 (plan-4 r3) — Round-3 quality: 8/8 closed as quoted and each proven live (launchd exports `XPC_SERVICE_NAME`, 8/8 plists carry the flag, beat 0–5 s fleet-wide, three watchdog restarts signalled at t=0, bug-013 wait+verify). The residue is again the surface nobody listed — the supervisor behind the deploy step — and a drift fixer that re-renders with whatever `rmx` ran it (no dev-tree refusal). "Restart" and "wedge" are indistinguishable in `hub status` (`restarts=1` on thiquet was a throttle-gap kick); read the launchd log before crediting either. {#imp-plan4-r3-progress}
- 2026-09-15 (plan-4 r4) — The plist SET is the unit, not the plist the finding named: `rmx=` + the dev-tree refusal reached `render_plist` and 8 daemon plists, and `render_hub_plist` — edited by the same commit, two lines below, to add `ExitTimeOut` — still renders from `which rmx` with no refusal and no `check` anywhere to notice. Same shape as `RMX_SUPERVISED` reaching 6/8 (r2). For any "every plist / every label / every supervisor" claim: enumerate every render function in the module, then loop `launchctl print` over every live label — that loop found the 10th label (session-indexer, `exit timeout = 5`) the plan's own table had already printed and not read. {#imp-plist-set-is-the-unit}
- 2026-09-15 (plan-4 r4) — A tri-state identity collapsed to a falsy test makes UNKNOWN safe: `binary_identity` returns `{"dev_tree": None, "error": …}` and documents "unknown, never 'fine'", while `if ident.get("dev_tree")` lets an old binary, a broken venv or a mistyped `--rmx` through — and the drift fixer then rewrites every plist to it. Any guard over a three-valued probe gets `is not False`, and the `error` it stored gets a reader. {#imp-tristate-unknown-is-safe}
- 2026-09-15 (plan-4 r4) — Best round in the lineage: 7/7 closed and the two that mattered closed in BEHAVIOUR — launchd's log has 0 SIGKILLs where it had 24, and a 29 s drain exited 0 under the new budget; `_await_shutdown` now reads a value its own guarded process still produces and its closing test drives `_check` on a real drain (guard-cannot-fire's three rules met); mock registry complete for the first time; the dev-tree test makes 0 launchctl calls under a real run (bug-023). The residue is again the unenumerated surface plus four call sites still holding the old default the plan replaced with "one number". Also: the tests write into the LIVE `~/.refmatrix/hub.log` (43 lines, 8 of them the new marker-wait line, which names no root) — an evidence surface that can no longer distinguish a test from a deploy. {#imp-plan4-r4-progress}
- 2026-09-15 (plan-2 r9) — An acceptance number taken 40 s after `relaunch-fleet` is the empty queue, not the path: the capped rerank pool measured 1.7–2.8 s at 05:38 and 3.72 / 9.26 / 14.08 s at 05:55 for the SAME docs on the SAME worker, and 0 of 7 deployed hook runs reranked under load 3.5–5.8. Wall-clock acceptance ("≤ budget + 0.6 s") passes either way, because a leg that always times out costs exactly its timeout. Ask what happened on the machine in the five minutes before any quoted cost, demand two runs minutes apart, and assert on the PRODUCT (`reranked` rows) not the wall. Memory: [[impression_bsd_acceptance_in_the_quiet_window]]. {#imp-acceptance-quiet-window}
- 2026-09-15 (plan-2 r9) — A budget handed whole to one leg is a budget denied to the next, and the loser's message blames the wrong component: the rerank takes `_l(30.0)` (the entire remainder) and every run then prints "global rows omitted … daemon busy; the hook skipped this turn" about a healthy global daemon that was never asked, while five rows print. For any multi-leg deadline, list the legs in order and ask which one can consume the whole thing; then read the failure text of every LATER leg for a cause it cannot actually know. {#imp-budget-whole-to-one-leg}
- 2026-09-15 (plan-2 r9) — A read-path fix applied to a shared resolver reaches the write path: `verbs.memory_partition` went replica-first for the hook probe and `memory_add` (MCP, bus, save-state) now takes its WRITE partition from a snapshot, in the function whose docstring names the split-brain family it exists to prevent. When a remedy changes a helper's source of truth, grep its callers and sort them into reads and writes before crediting the scope; the signature here already distinguishes them (`timeout is not None`). {#imp-read-fix-reaches-write-path}
- 2026-09-15 (plan-3 r6) — The twin inventory the r5 impression asked for was never taken: `list` and `search` were routed through the verb, and the same `rmx memory` group's `promote` (180.2 s, empty message) and `get --degree` (a bare `NameError` on the deployed build, since the r5 remedy dropped an import thirty lines above the call) went untouched and untested. When a remedy rewrites a command body, diff the WHOLE function, not the hunk: an import removed at the top is a crash at the bottom, and `python -c "import cli"` will not find it. Enumerate a command group's subcommands from the click tree and run EVERY ONE of them once, with each of its flags, against the held-writer fixture — the flag with no test is the flag that is already broken. {#imp-whole-function-diff}
- 2026-09-15 (plan-3 r6) — Round-6 quality: 5/5 closed and, for the first time in this plan, closed on the simulation each finding named (four daemon states probed, slot untouched in all of them, my four mutations each fail the tests that claim them, registry complete at last). The residue is again the unenumerated member of a universal: "every fan-out" is four fan-outs and the fourth turns a busy store into "no live project hosts"; three of four new anti-silence branches in `federated_where` can be deleted with the suite green. A mutation that deletes the guard, not the behaviour, is the cheapest test of a "said, never mute" claim — run it on every branch the claim covers, not on the one the remedy's own table names. {#imp-plan3-r6-progress}
- 2026-09-16 (plans 7-10 r1) — THE DOCSTRING IS THE FASTEST PLACE TO FIND THE LIE. This codebase writes docstrings that name the incident a module exists to prevent, and six of eight BULLSHIT findings this round were exactly that named incident, unfixed, inside the module that cites it: `brief.py` invokes `feedback_reuse_shared_stoplist` ("four call sites, one definition is the fix that stuck") and emits `the` as its top production brief; it excludes operational rows because "they dominate every class" and does not exclude `brief/*`, so saved briefs come back as singletons about themselves; `parse_gmd` says "the store is rebuildable from the index" and drops `evidence`, the field `__post_init__` calls the whole contract. New standing move: for every new module, list each claim its docstring makes about itself, then write the one-line probe that falsifies it — before reading a single test. {#imp-docstring-is-the-lie}
- 2026-09-16 (plans 7-10 r1) — RUN THE DETECTOR ON THE REAL CORPUS, NOT ON THE FIXTURE. `contradicted` passes six tests over hand-built memory↔memory fragments and emits ZERO on this project's own store, because the `contradicts` linkage connects GMD `#anchor` concept nodes (54 of 59 endpoints) and the detector requires `kind='memory'` on both sides — 41 in, 41 skipped, 0 out. The probe that found it costs three minutes: `cp .refmatrix/catalog.read.duckdb $TMP/.refmatrix/catalog.duckdb` and run the new function against it. Any detector, scorer, or coverage pass gets that copy-and-run before its assertions are read; "skipped: N with emitted: 0" is the signature. {#imp-run-on-the-real-corpus}
- 2026-09-16 (plans 7-10 r1) — A METRIC'S POPULATION IS A CLAIM AND IT IS NEVER CHECKED. `hook_budget` calls its windows "one prompt's hook fan-out"; on the live cli.log 10,740 windows contain 1,271 scan-prompts (11.8%) because every hook template exports `RMX_INVOCATION_SOURCE=hook` and PostToolUse `focus hook` is 75% of the rows. Same round: `--depth 50` recorded in `_meta` and dropped from `scan`'s argv; `out_bytes` called "exactly what the hook injected" while counting bytes sent to `/dev/null`. For any new aggregate: name the population, then COUNT it on production data and compute what fraction actually belongs — before reading the p50. {#imp-metric-population-unchecked}
- 2026-09-16 (plans 7-10 r2) — MUTATION IS THE ONLY CREDIT-WORTHY PROOF, AND IT CUTS BOTH WAYS. Reverting each claimed fix and re-running took four minutes and settled five findings honestly: `--gmd` and `brief/*` are genuinely guarded (a test fails), while `test_brief_real_daemon.py` — a REAL spawned daemon, short root, socket asserted — passes under BOTH mutations, because `rmx memory add` writes no linkage fragments so every fixture yields zero briefs and the empty-document branch is all that runs. A real daemon is not real coverage. Standing move: before crediting any "live"/"real-X" test file, mutate the line it exists to protect; and when a fixture is supposed to produce output, assert the output is NON-EMPTY in the fixture itself. {#imp-mutation-cuts-both-ways}
- 2026-09-16 (plans 7-10 r2) — A REMEDY THAT FOLLOWS MY WORDING EXACTLY IS THE ONE TO RE-MEASURE. Two of five opens are my own r1 sentence, implemented verbatim, still wrong: "resolve the endpoint to any entity" produced 47 briefs of which 45 are ch-bsd's own `rel: contradicts` edges with a finding line still reading "two memories"; "pass a depth equivalent to scan-prompt" produced `--max-tokens`, which is INERT on `--format json` (budget 1 and 100,000 both emit 11,931 bytes; text mode 2,754 → 6,059). I named the mechanism and did not measure the flag. New rule for my own advice: when a finding proposes a specific flag or call, probe that flag in the exact output mode the caller uses BEFORE writing it into the report — otherwise the next round audits my own guess. {#imp-my-fix-needs-measuring}
- 2026-09-16 (plans 7-10 r2) — "FIXED, 0 NOW" CAN BE VACUOUS ON BOTH SIDES OF THE COMPARISON. The hook-budget remedy reported "0 prompt-anchored windows instead of 10,740", but `counted == 0` on the live log (no row has `out_bytes` pre-deploy) so the OLD code also returns 0 — the figure proved nothing. Replaying 23,662 real hook rows with measured per-command byte sizes showed the fix is genuinely good (10,802 → 1,273 windows, p50 0 → 4,370 B). When a before/after number contains a 0, check whether the population was empty for an unrelated reason, then BUILD the data that exercises the path rather than accepting either number. {#imp-zero-on-both-sides}
- 2026-09-16 (plans 7-10 r3) — A RENDERER CAN DESTROY A CORRECT PAYLOAD, AND THE LINTER WILL BLESS THE WRECKAGE. The `--gmd` names map was fixed correctly at the daemon and in `render_gmd`, then `console.print` parsed `[[note-1]]` as rich markup: the user receives `Evidence (2): [] `1`` and 8 × `rel: evidence-for -> []`. `tools/gmd/lint.py` reports 0 errors on that file, because `[[]]` is not a wikilink at all — so the "GMD lint zero errors" commit gate passes on a document whose entire edge set was deleted. For any command that emits STRUCTURED text (GMD, JSON, YAML, a patch), assert on the BYTES THROUGH THE CLI, never on the builder's return value; and remember a linter that only reports malformed constructs cannot report absent ones. `click.echo` for machine output, `console.print` for humans. {#imp-renderer-destroys-payload}
- 2026-09-16 (plans 7-10 r3) — THE FAKE THAT OMITS THE NEW FIELD IS THE BLINDFOLD. `--gmd` went through a `CliRunner` test the whole time, and it could not see the mangling because its `fake_memory` returned no `names` key — so no wikilink existed to destroy, and the assertion was `"gmd:" in res.output`. The test that asserted `[[lonely]]` ran in-process. When a fix ADDS a field to a payload, grep every fake that constructs that payload and add the field, or the CLI test is guaranteed to exercise the pre-fix shape forever. Corollary seen the same round: a claimed-closed gap ("can't observe it without a second writer") was closable by polling the daemon — before accepting an honest-gap declaration, spend five minutes looking for the read-only route. {#imp-fake-omits-the-new-field}
- 2026-09-16 (plans 7-10 r4) — AN EDIT TO A GMD DOC IS A GRAPH MUTATION; PARSE IT BEFORE AND AFTER. The commit that fixed `console.print` eating every `[[link]]` appended one line past a closing fence and added a second fence, taking `eval/.../REPORT.md` from 2 fences to 3 — so `ingest_gmd.parse_gmd` (and `tools/gmd/lint.py`, same `in_code` toggle) lost the `#next` node: 12 anchor ids at `4f451c5`, 11 at HEAD. No gate saw it: `eval/` is not in `scripts/lint-gmd.sh` SCOPE, and a linter that skips fenced content cannot report a heading it was told is code. New standing move for any diff touching a `gmd: "0.1"` file: run `parse_gmd` on both revisions and diff the node-id lists; an odd `^```` count is a one-line check nobody has written. {#imp-doc-edit-is-a-graph-mutation}
- 2026-09-16 (plans 7-10 r4) — THE FIELD NEXT TO THE ONE I NAMED IS THE ONE STILL BROKEN. r2 `#b-2-r2d` was "`--gmd` renders `(unresolved)` for EVERY evidence id because the caller does not pass `names`"; it was fixed by passing `names=`, and the SAME call still omits `stats=`, so every real `--gmd` doc says "an unrecorded number of memories" over a corpus of 243 while `--json` and the table both print the number. The `stats` parameter has no production caller and no test passes it. When a finding is "caller drops field X of payload P", read the whole call signature and check EVERY optional field of P against what the caller already holds in scope — the fix that passes exactly the one field I named leaves the sibling firing its honest-fallback at 100%. {#imp-field-next-to-the-one-i-named}
- 2026-09-16 (plans 7-10 r4) — AN UNATTRIBUTABLE FAILURE WHOSE ASSERTION READS SOURCE TEXT MEANS THE FILE CHANGED MID-RUN. The "sixth failure" everyone called a flake was `inspect.getsource(dm.serve_foreground)` returning one line from a different function: `linecache.checkcache` re-reads the edited file and slices it with the ALREADY-LOADED code object's `co_firstlineno`, so a +5-line edit at `daemon.py:4305` (commit 01:00:00) reinterpreted a suite that started ~00:48. Reproduced byte-for-byte in 30 seconds. Standing move: before calling any failure a flake, check whether the assertion reads source/disk state, then diff the commit times against the run window — and run it standalone at HEAD (3/3, 0.78 s) to separate "code is fine" from "harness is fine". {#imp-getsource-mid-run-edit}

## Former anchors, restored {#former-anchors}

Consolidation moved this ledger's per-run impressions into dedicated memories under
`memory/impressions/` and collapsed their anchors. The historical reports that cite them were never
edited — correctly, an audit report is a record — so 97 `dangling-anchor` errors accumulated and
`CLAUDE.md#before-commit` ("GMD lint zero errors") had not been green for days. Each id below is
restored as an addressable node so its inbound edges resolve; the content lives where the line says.
Anchors whose destination is not individually evidenced point at [[#imp-consolidated-0914]] and say
so rather than guessing a file (ch-bsd r4 remedy).

- `imp-busy-absent-call-tree` → [[impression_bsd_busy_absent_call_tree]]. {#imp-busy-absent-call-tree}
- `imp-cost-measured-idle` → [[impression_bsd_cost_measured_idle]]. {#imp-cost-measured-idle}
- `imp-existence-check-tests` → [[impression_bsd_existence_check_tests]]. {#imp-existence-check-tests}
- `imp-quoted-line-sibling` → [[impression_bsd_quoted_line_sibling]]. {#imp-quoted-line-sibling}
- `imp-tests-bypass-wiring` → [[impression_bsd_tests_bypass_wiring]]. {#imp-tests-bypass-wiring}
- `imp-unread-diagnostic-field` → [[impression_bsd_unread_diagnostic_field]]. {#imp-unread-diagnostic-field}
- `imp-grace-before-signal` → [[impression_bsd_grace_spent_before_signal]]. {#imp-grace-before-signal}
- `imp-partial-bound` → [[impression_bsd_partial_bound_guard]], amended by [[impression_bsd_partial_bound_guard_2]]. {#imp-partial-bound}
- `imp-silent-memory` → [[impression_bsd_silent_memory_paths]]. {#imp-silent-memory}
- `imp-registry-2-strikes` → [[impression_bsd_registry_row_deferral]]. {#imp-registry-2-strikes}
- `imp-registry-three-strikes` → [[impression_bsd_registry_row_deferral]], same lineage one strike later. {#imp-registry-three-strikes}
- `imp-consolidated-1` — listed by name in [[#imp-consolidated-0914]]. {#imp-consolidated-1}
- `imp-consolidated-2` — listed by name in [[#imp-consolidated-0914]]. {#imp-consolidated-2}
- `imp-fix-at-quoted-line` — listed by name in [[#imp-consolidated-0914]]; the live form is [[impression_bsd_quoted_line_sibling]]. {#imp-fix-at-quoted-line}
- `imp-socket-sim-probe` — listed by name in [[#imp-consolidated-0914]]. {#imp-socket-sim-probe}
- `imp-busy-absent-verb-layer` — busy≠absent at the verb layer; consolidated, see [[#imp-consolidated-0914]]. {#imp-busy-absent-verb-layer}
- `imp-fleet-sum-timeouts` — a fan-out's bound is the SUM of its legs' timeouts; consolidated, see [[#imp-consolidated-0914]]. {#imp-fleet-sum-timeouts}
- `imp-guard-vs-incident-state` — guards built for the fixed state, not the incident; consolidated, see [[#imp-consolidated-0914]]. {#imp-guard-vs-incident-state}
- `imp-migration-no-tests` — verb migrations shipped without tests; consolidated, see [[#imp-consolidated-0914]]. {#imp-migration-no-tests}
- `imp-mock-the-sut` — a test that monkeypatches the function under test; consolidated, see [[#imp-consolidated-0914]]. {#imp-mock-the-sut}
- `imp-plan3-r3-progress` — plan-3 round-3 progress note; consolidated, see [[#imp-consolidated-0914]]. {#imp-plan3-r3-progress}
- `imp-silent-pass-migrates` — a silent pass carried across a migration; consolidated, see [[#imp-consolidated-0914]]. {#imp-silent-pass-migrates}
- `imp-summary-claims-bookkeeping` — a summary claiming the bookkeeping instead of the behaviour; consolidated, see [[#imp-consolidated-0914]]. {#imp-summary-claims-bookkeeping}
- `imp-two-paths` — two ways to do X, one of them unwired; consolidated, see [[#imp-consolidated-0914]]. {#imp-two-paths}
- `imp-verbatim-fix` — a remedy implementing my one-line wording verbatim and still wrong; the live form is [[impression_bsd_my_fix_needs_measuring]]. {#imp-verbatim-fix}
